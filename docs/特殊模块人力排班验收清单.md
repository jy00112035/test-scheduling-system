# 特殊模块人力排班验收清单

## 验收信息

| 项目 | 填写内容 |
| --- | --- |
| 验收版本/提交 |  |
| 验收环境 |  |
| 浏览器及版本 |  |
| 验收人 |  |
| 验收日期 |  |
| 测试数据前缀 | `UAT-SM-` |

结果填写规则：每项仅勾选一个结果。失败或阻塞时，必须在“实际结果”中记录现象，并在“证据/引用”中附截图、录屏、请求响应或缺陷编号。

## 通用前置条件

- [ ] 前后端均使用待验收版本启动，页面可正常登录和访问。
- [ ] 已准备字段管理员、测试经理/项目经理、资源经理（或具备等效权限）的测试账号。
- [ ] 已确认验收数据库可回滚或为一次性测试库；测试数据统一使用 `UAT-SM-` 前缀。
- [ ] 浏览器开发者工具可查看网络请求，截图或录屏目录已确定。
- [ ] 验收前记录当前模块、人员、需求和排班基线；验收后清理本次测试数据。

## 浏览器验收流程

| 编号 | 工作流 | 专项前置条件 | 操作步骤 | 预期结果 | 实际结果与结论 | 证据/引用 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 模块名称唯一性 | 以字段管理员登录；准备名称 `UAT-SM-支付` | 1. 在模块配置中新建该模块。<br>2. 再次新建同名模块，可在名称前后加入空格验证规范化。 | 首次创建成功；第二次提交被拒绝，页面显示明确的重复名称原因；列表中只存在一条该名称记录。 | 实际：首次创建成功；带首尾空格的同名提交显示“模块名称已存在”，列表仅一条。<br>[x] 通过 [ ] 失败 [ ] 待执行 [ ] 阻塞 | 2026-07-23，一次性 H2 浏览器环境；模块列表及重复名称页面提示。 |
| 2 | 跨组人员熟悉模块 | 已有启用的“功能测试”模块；以可编辑人员的角色登录 | 1. 新建或编辑一名“性能测试”人员。<br>2. 将“功能测试”模块加入其熟悉模块并保存。<br>3. 刷新页面并重新打开编辑框。 | 跨组模块可被选择并保存；列表和编辑框均显示结构化模块名称；刷新后关系不丢失。 | 实际：性能测试人员 `UATSM001` 成功保存并显示“功能测试: UAT-SM-支付”。<br>[x] 通过 [ ] 失败 [ ] 待执行 [ ] 阻塞 | 2026-07-23，一次性 H2 浏览器环境；人员列表及编辑回填。 |
| 3 | 特殊模块人力超额 | 已有“功能测试”模块；进入需求新建或编辑页 | 1. 设置功能测试总人力为 `1.0`。<br>2. 将一个或多个特殊模块人力合计设置为 `1.5`。<br>3. 尝试提交。 | 页面阻止提交并定位特殊模块区域；提示特殊模块人力合计不能超过所属小组总人力；后端拒绝绕过页面的超额请求。 | 实际：页面显示“特殊模块人力不能超过小组总人力”和“功能测试的小组特殊模块人力不能超过总人力”，表单未提交。<br>[x] 通过 [ ] 失败 [ ] 待执行 [ ] 阻塞 | 2026-07-23，一次性 H2 浏览器环境；表单校验提示。 |
| 4 | 审批页特殊模块展示 | 已提交一条包含特殊模块人力的待审批需求 | 1. 进入需求审批页。<br>2. 查看需求详情。<br>3. 打开“修改并批准”并检查特殊模块字段。 | 审批列表/详情显示模块名称、所属测试类型和人力；修改框回填准确；停用但历史已引用的模块仍可识别，不显示为未知值。 | 实际：<br>[ ] 通过 [ ] 失败 [x] 待执行 [ ] 阻塞 | 审批页截图；需求详情响应： |
| 5 | 指定日期范围推荐 | 已批准需求；至少一名具备特殊模块能力且有容量的人员 | 1. 在排班工作台选择需求。<br>2. 选择“指定日期范围”，范围取需求周期子集。<br>3. 运行推荐。 | 仅在指定范围与需求周期交集中生成草稿；特殊草稿带模块归属并分配给熟悉该模块的人员；满足情况和缺口原因可见。 | 实际：<br>[ ] 通过 [ ] 失败 [x] 待执行 [ ] 阻塞 | 工作台截图；推荐请求/响应： |
| 6 | 完整需求周期推荐 | 已批准需求；特殊人力为 `0.5` 的单日或多日需求；人员容量充足 | 1. 选择“完整需求周期”。<br>2. 运行推荐。<br>3. 查看草稿详情与履约摘要。 | 推荐覆盖完整需求周期；特殊模块优先分配；草稿的人员、日期、比例、通用明细归属和特殊模块归属准确；履约结果为已满足。 | 实际：<br>[ ] 通过 [ ] 失败 [x] 待执行 [ ] 阻塞 | 草稿详情截图；推荐响应： |
| 7 | 手工排班拒绝与接受 | 已批准需求；准备一名不熟悉模块人员和一名熟悉模块人员 | 1. 尝试将特殊模块草稿移动/新建到不熟悉人员。<br>2. 再将其移动/新建到熟悉人员。<br>3. 刷新工作台。 | 不熟悉人员的操作被拒绝并显示明确原因；熟悉人员的操作成功；刷新后人员与模块归属保持一致。 | 实际：<br>[ ] 通过 [ ] 失败 [x] 待执行 [ ] 阻塞 | 两次操作截图；校验/保存响应： |
| 8 | 详细履约与待归类识别 | 准备含特殊人力的需求，并准备一条历史排班，其通用明细和特殊模块归属均为空 | 1. 打开需求详细履约视图。<br>2. 检查特殊、通用、总计和待归类提示。<br>3. 尝试发布。 | 详细履约分别显示特殊模块与通用人力的需求、已排和缺口；历史双空排班被标为待归类；发布被阻止并提示 `SCHEDULE_HISTORICAL_CLASSIFICATION_REQUIRED`。 | 实际：<br>[ ] 通过 [ ] 失败 [x] 待执行 [ ] 阻塞 | 履约面板截图；发布响应： |
| 9 | 能力移除后的发布阻断 | 已通过推荐生成未发布的特殊模块草稿 | 1. 在人员管理中移除该人员的对应熟悉模块并保存。<br>2. 返回工作台刷新。<br>3. 对原需求执行发布。 | 发布按最新能力重新校验并失败；原因码为 `STAFF_MODULE_NOT_FAMILIAR`；该需求没有任何排班被发布；失败原因可追溯。 | 实际：<br>[ ] 通过 [ ] 失败 [x] 待执行 [ ] 阻塞 | 人员变更及发布截图；审计/响应： |
| 10 | 批量发布部分成功及原因 | 准备需求 A（全部合法）和需求 B（推荐后移除人员模块能力），两者均有草稿 | 1. 同时勾选 A、B 执行批量发布。<br>2. 查看汇总和逐需求结果。<br>3. 刷新后查看已发布状态。 | 批量请求整体完成；A 成功并显示发布条数，B 失败并显示 `STAFF_MODULE_NOT_FAMILIAR` 及可读原因；A 的排班均已发布，B 的排班均未发布。 | 实际：<br>[ ] 通过 [ ] 失败 [x] 待执行 [ ] 阻塞 | 批量结果截图；请求响应；审计： |

## 环境与数据库验证

| 验证项 | 预期 | 实际结果与结论 | 证据/引用 |
| --- | --- | --- | --- |
| H2 现有文件升级/重启 | 对仓库外副本启动；Flyway 到达 V3；Hibernate 校验通过；需求、人员、排班代表性读取成功；仓库内原文件不变。 | 实际：将非空 `backend/data/testdb.mv.db` 复制到仓库外临时目录后启动；Flyway 从 V1 升至 V3，JPA 初始化成功；需求、人员、排班接口均返回 HTTP 200 和非空结果；原文件 SHA-256 保持不变。<br>[x] 通过 [ ] 失败 [ ] 待执行 [ ] 阻塞 | 2026-07-23 启动日志；`/api/demands` 6112 字节、`/api/staff` 50348 字节、`/api/schedules` 2018 字节；原文件校验值 `57139529e3229c4122560743c123f27581d1c7b4c6a6f50539fa59e818f2f5e9`。 |
| MySQL 8 空库迁移/启动 | 使用外部注入的 `MYSQL_URL`、`MYSQL_USERNAME`、`MYSQL_PASSWORD` 和 `JWT_SECRET`；空库迁移到 V3，Hibernate 校验通过。 | 当前环境未提供 `MYSQL_URL`、`MYSQL_USERNAME`、`MYSQL_PASSWORD`，且检测到的本机服务端为 MySQL 9.6.0，并非要求的 MySQL 8。<br>[ ] 通过 [ ] 失败 [ ] 待执行 [x] 阻塞 | 限制：缺少外部凭据与 MySQL 8 一次性空库；未创建或修改任何 MySQL 数据库。 |

## 验收汇总

| 结论 | 勾选 |
| --- | --- |
| 全部通过，可发布 | [ ] |
| 有条件通过，限制已记录 | [x] |
| 不通过，需修复后复验 | [ ] |

遗留问题/缺陷编号：

1. 浏览器流程 4–10 尚未执行，保持待执行状态，不作为自动化测试通过的替代结论。
2. 当前环境缺少 MySQL 8 一次性空库及外部注入凭据，MySQL 迁移/启动验证阻塞。

验收人签字：____________________　日期：____________________

复核人签字：____________________　日期：____________________
