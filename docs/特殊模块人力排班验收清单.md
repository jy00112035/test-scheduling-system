# 特殊模块人力排班验收清单

## 验收信息

| 项目 | 填写内容 |
| --- | --- |
| 目标版本/提交 | `6823e3336e0d7f02ed2a913fcfa38aead74c0cf0` |
| 验收日期 | 2026-07-23 |
| 环境/测试上下文 | 本地 macOS；自动化门禁使用测试 H2；H2 运行时升级使用仓库外临时副本；浏览器人工验收尚未形成可复验证据。 |
| 浏览器及版本 | 待指定 |
| 验收操作人 | 待指定 |
| 测试数据前缀 | `UAT-SM-` |
| 当前结论 | **验收未完成/待复验** |

结果填写规则：每个浏览器工作流只有在“证据引用”列要求的页面截图和相关请求/响应（或后端错误码）均可复验时，才可勾选“通过”。口头观察、临时页面状态或未保存的终端输出不能作为通过证据。

## 通用前置条件

- [x] 目标提交 `6823e3336e0d7f02ed2a913fcfa38aead74c0cf0` 已确定，自动化测试上下文已记录。
- [x] 已使用仓库外一次性 H2 副本完成运行时升级验证，仓库内原数据库文件未被修改。
- [ ] 已指定浏览器、版本和验收操作人。
- [ ] 已准备字段管理员、人员管理员、测试经理/项目经理、审批人和排班操作人等所需角色。
- [ ] 已建立持久化证据目录或缺陷系统引用，能够保存每项截图及请求/响应。
- [ ] 已在可回滚或一次性验收数据库中准备 `UAT-SM-` 数据，并记录验收前基线和验收后清理结果。

## 浏览器验收流程

| 编号 | 工作流 | 专项前置条件 | 操作步骤 | 预期结果 | 状态/结果 | 复选 | 证据引用 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 两个不同小组模块及名称唯一性 | 以字段管理员登录；准备两个不同测试小组和一个全局唯一候选名称 | 1. 在小组 A 新建模块 `UAT-SM-A`。<br>2. 在小组 B 新建模块 `UAT-SM-B`。<br>3. 在任一小组再次提交与现有模块同名的名称，并验证首尾空格规范化。 | 两个不同小组的不同名称模块均创建成功；任何小组提交重复名称均被拒绝；列表中重复名称仅保留原记录。 | 待验；未提交可复验的浏览器与接口证据。 | [ ] 通过 [ ] 失败 [x] 待验 | 通过必需：模块列表截图、重复名称错误截图，以及两次成功创建和重复创建的相关请求/响应或后端重复名称错误码。 |
| 2 | 跨组人员熟悉模块 | 已有启用的功能测试模块；以人员管理员登录；准备其他小组测试员 | 1. 新建或编辑其他小组测试员。<br>2. 选择功能测试模块作为熟悉模块并保存。<br>3. 刷新列表并重新打开编辑页。 | 跨组模块可选择、保存并按结构化模块名称回填；刷新后人员与模块关系不丢失。 | 待验；未提交可复验的浏览器与接口证据。 | [ ] 通过 [ ] 失败 [x] 待验 | 通过必需：人员编辑和回填截图，以及保存请求/响应和刷新后的 `GET /api/staff` 相关响应。 |
| 3 | 小组总量、多特殊模块行及溢出校验 | 以测试经理登录；同一测试类型下至少有两个启用模块 | 1. 设置小组总人力。<br>2. 新增多个特殊模块人力行并确认通用人力自动计算。<br>3. 将特殊模块合计调至超过小组总人力并从页面提交。<br>4. 绕过页面直接发送同等超额的创建或修改请求。 | 多特殊模块行可正确录入；特殊合计不超额时通用人力等于小组总量减特殊合计；超额时页面阻止提交；绕过页面的请求也被后端拒绝且不持久化。 | 待验；缺少后端绕过请求的持久证据。 | [ ] 通过 [ ] 失败 [x] 待验 | 通过必需：总量、多行、通用人力及页面溢出提示截图，以及超额 `POST/PUT /api/demands` 请求体和后端拒绝响应或错误码。 |
| 4 | 审批总量、特殊与通用人力展示及持久化 | 已提交包含小组总量、多个特殊模块和通用人力的待审批需求 | 1. 打开审批列表和详情。<br>2. 核对小组总量、各特殊模块人力和通用人力。<br>3. 批准需求并刷新或重新进入详情。 | 审批前展示的总量、特殊模块明细和通用人力计算正确；批准后状态更新，全部人力拆分仍准确持久化且不丢失。 | 待验；未提交可复验的审批前后证据。 | [ ] 通过 [ ] 失败 [x] 待验 | 通过必需：审批前后页面截图，以及批准请求/响应和批准后需求详情响应。 |
| 5 | 指定日期范围推荐仅分配熟悉人员 | 已批准特殊模块需求；准备同测试类型的熟悉人员和不熟悉人员，且两者均有容量 | 1. 选择“指定日期范围”并取需求周期子集。<br>2. 运行推荐。<br>3. 核对特殊模块草稿的日期、人员和模块归属。 | 仅在指定范围与需求周期交集生成草稿；特殊模块只分配给熟悉该模块的人员，不熟悉人员不得获得该特殊模块草稿。 | 待验；未提交可复验的推荐证据。 | [ ] 通过 [ ] 失败 [x] 待验 | 通过必需：范围选择和推荐结果截图，以及推荐请求/响应中日期、`staffId`、`demandSpecialModuleId` 与人员熟悉模块关系的证据。 |
| 6 | 完整需求推荐在单个特殊模块无可用人员时继续排其他工作 | 已批准需求，包含至少两个特殊模块和通用人力；其中一个特殊模块无人符合，其他模块和通用人力有合格人员 | 1. 选择“完整需求周期”。<br>2. 运行推荐。<br>3. 查看各特殊模块和通用人力草稿及缺口。 | 无合格人员的特殊模块保留明确缺口；推荐不中断，其他特殊模块和通用人力继续生成合法草稿；需求整体保持未完全满足。 | 待验；未提交可复验的部分推荐和缺口证据。 | [ ] 通过 [ ] 失败 [x] 待验 | 通过必需：模块级履约和草稿截图，以及完整推荐请求/响应，能同时证明一个模块缺口、其他模块成功和通用人力成功。 |
| 7 | 手工排班不熟悉拒绝、熟悉接受 | 已批准特殊模块需求；准备一名不熟悉人员和一名熟悉人员 | 1. 将特殊模块任务拖动或编辑给不熟悉人员。<br>2. 再将其拖动或编辑给熟悉人员。<br>3. 刷新工作台。 | 不熟悉人员的操作被拒绝并回弹或恢复原值；熟悉人员的操作成功；刷新后合法人员与模块归属保持一致。 | 待验；未提交两次操作的可复验证据。 | [ ] 通过 [ ] 失败 [x] 待验 | 通过必需：拒绝和接受后的页面截图，以及两次校验/保存请求与响应；拒绝响应需包含相关后端错误码。 |
| 8 | 总量已满足但特殊模块仍有缺口时保持待排期 | 已批准需求；安排通用或其他排班使总人力数值达到需求总量，但至少一个特殊模块人力仍未满足 | 1. 打开详细履约视图。<br>2. 核对总量、通用人力和各特殊模块的已排及缺口。<br>3. 刷新需求分类或状态。 | 即使总量数值已达到，未满足的特殊模块仍显示缺口；需求不得归入“已分配”，并保持“待排期”。 | 待验；不得用历史归属分类场景替代本项。 | [ ] 通过 [ ] 失败 [x] 待验 | 通过必需：总量已满足、特殊模块仍缺口和“待排期”状态同屏或连续截图，以及详细履约/需求列表相关请求与响应。 |
| 9 | 移除人员能力后由后端阻止发布 | 已推荐生成未发布的特殊模块草稿，且草稿人员原本熟悉该模块 | 1. 移除该人员对应熟悉模块并保存。<br>2. 返回工作台刷新。<br>3. 发布原需求。 | 发布按最新能力重新校验并失败；后端返回 `STAFF_MODULE_NOT_FAMILIAR`；该需求没有排班被发布，失败原因可追溯。 | 待验；自动化覆盖不能替代浏览器验收证据。 | [ ] 通过 [ ] 失败 [x] 待验 | 通过必需：能力移除和发布失败截图，以及人员更新请求/响应、发布请求/响应中的 `STAFF_MODULE_NOT_FAMILIAR` 和发布后排班查询。 |
| 10 | 批量发布部分成功及逐需求原因 | 准备需求 A（全部合法）和需求 B（推荐后移除人员模块能力），两者均有草稿 | 1. 同时选择 A、B 执行批量发布。<br>2. 查看汇总和逐需求结果。<br>3. 刷新后核对已发布状态。 | 批量请求整体完成；A 成功并显示发布条数；B 失败并显示 `STAFF_MODULE_NOT_FAMILIAR` 及可读原因；A 均已发布，B 均未发布。 | 待验；自动化覆盖不能替代浏览器验收证据。 | [ ] 通过 [ ] 失败 [x] 待验 | 通过必需：批量结果和刷新后状态截图，以及批量发布请求/响应、逐需求结果和发布后 A/B 排班查询。 |

## 自动化与技术门禁

| 门禁 | 预期 | 实际/结果 | 提交 | 日期 | 可复验证据引用 | 复选 |
| --- | --- | --- | --- | --- | --- | --- |
| 服务集成与后端全量测试 | `cd backend && mvn clean test` 成功，零失败、零错误 | 305/305 通过，0 failures，0 errors，0 skipped。 | `6823e3336e0d7f02ed2a913fcfa38aead74c0cf0` | 2026-07-23 | 命令：`cd backend && mvn clean test`；报告：`backend/target/surefire-reports/`；核心场景：`SpecialModuleSchedulingIntegrationTest`；迁移：`MigrationSmokeTest`。 | [x] 通过 [ ] 失败 [ ] 待验 |
| 前端单元测试 | `npm test` 全部通过 | 19 个测试文件、148/148 测试通过。 | `6823e3336e0d7f02ed2a913fcfa38aead74c0cf0` | 2026-07-23 | 命令：`npm test`；测试源：`src/**/*.test.{ts,tsx}`。 | [x] 通过 [ ] 失败 [ ] 待验 |
| 前端静态检查 | `npm run lint` 零错误 | 退出码 0。 | `6823e3336e0d7f02ed2a913fcfa38aead74c0cf0` | 2026-07-23 | 命令：`npm run lint`；配置：`eslint.config.js`。 | [x] 通过 [ ] 失败 [ ] 待验 |
| 前端构建 | `npm run build` 成功 | TypeScript 和 Vite 构建成功；存在非阻断的大包体提示。 | `6823e3336e0d7f02ed2a913fcfa38aead74c0cf0` | 2026-07-23 | 命令：`npm run build`；产物目录：`dist/`。 | [x] 通过 [ ] 失败 [ ] 待验 |
| 旧算法/旧类型门禁 | 下方既定 `rg` 门禁命令无输出 | 无匹配，退出状态符合无匹配预期。 | `6823e3336e0d7f02ed2a913fcfa38aead74c0cf0` | 2026-07-23 | 重跑下方“旧算法/旧类型门禁命令”。 | [x] 通过 [ ] 失败 [ ] 待验 |
| H2 现有文件运行时升级 | 仓库外副本升级至 V3；JPA validate 成功；代表性接口可读；原文件不变 | 非空 H2 副本由 V1 升至 V3，JPA 初始化成功；需求、人员、排班接口均 HTTP 200 且非空；原文件校验值不变。 | `6823e3336e0d7f02ed2a913fcfa38aead74c0cf0` | 2026-07-23 | 自动回归：`MigrationSmokeTest`；持久化证据：[H2 运行时升级验证记录](verification/2026-07-23-task14-h2-runtime-upgrade.md)。 | [x] 通过 [ ] 失败 [ ] 待验 |

旧算法/旧类型门禁命令：

```bash
rg -n "runAllocationCore|familiarModules\?: string" src
```

## MySQL 8 验证

| 验证项 | 预期 | 实际/结果 | 提交 | 日期 | 可复验证据引用 | 复选 |
| --- | --- | --- | --- | --- | --- | --- |
| MySQL 8 空库迁移与启动 | 通过外部注入的 `MYSQL_URL`、`MYSQL_USERNAME`、`MYSQL_PASSWORD`、`JWT_SECRET` 连接一次性空 MySQL 8 schema；迁移到 V3，JPA validate 和应用启动成功 | 待验。仅观察到 MySQL 客户端 9.6.0；未验证 MySQL 8 服务端或一次性空 schema；四个环境变量均不可用；未创建或修改任何 MySQL schema。 | `6823e3336e0d7f02ed2a913fcfa38aead74c0cf0` | 2026-07-23 | 通过必需：不含密钥的客户端/服务端版本记录、空 schema 标识、Flyway V1-V3 日志和 JPA/application startup 日志。不得在仓库记录真实凭据。 | [ ] 通过 [ ] 失败 [x] 待验 |

## 验收结论

| 结论 | 复选 |
| --- | --- |
| 验收完成，可发布 | [ ] |
| 有条件通过 | [ ] |
| **验收未完成/待复验** | [x] |

待复验项：

1. MySQL 8 空库迁移与启动验证。
2. 浏览器人工验收工作流 1-10，逐项补齐页面截图和相关请求/响应或后端错误码。

验收操作人签字：____________________　日期：____________________

复核人签字：____________________　日期：____________________
