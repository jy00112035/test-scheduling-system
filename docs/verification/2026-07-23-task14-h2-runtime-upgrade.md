# Task 14 H2 运行时升级验证记录

## 验证上下文

| 项目 | 记录 |
| --- | --- |
| 日期 | 2026-07-23 |
| 目标提交 | `6823e3336e0d7f02ed2a913fcfa38aead74c0cf0`（`6823e33`） |
| 验证范围 | 非空 H2 文件从既有 V1 状态升级到 V3、JPA schema 校验、代表性只读接口和进程清理 |
| 数据隔离 | 仅对仓库外临时副本启动应用；未直接使用或迁移仓库中的源数据库文件 |

本记录只保留可复验的技术事实，不包含绝对本地路径、端口、凭据、认证密钥值或数据库原始内容。

## 源文件完整性

非空源 H2 文件在复制前和验证完成后分别计算 SHA-256：

| 检查点 | SHA-256 | 结果 |
| --- | --- | --- |
| 复制前 | `57139529e3229c4122560743c123f27581d1c7b4c6a6f50539fa59e818f2f5e9` | 基线 |
| 验证后 | `57139529e3229c4122560743c123f27581d1c7b4c6a6f50539fa59e818f2f5e9` | 与基线一致 |

升级产生的数据库写入仅发生在仓库外副本。源文件校验值前后一致，仓库中没有新增或修改 H2 数据库制品。

## 启动与迁移证据

1. 将非空源 H2 文件复制到仓库外临时目录，并让后端数据源仅指向该副本。
2. Flyway 识别既有 schema，将其 baseline 为 V1。
3. Flyway 依次执行 V2 和 V3，最终 schema 版本为 V3。
4. Hibernate/JPA 成功初始化 `EntityManagerFactory`；配置使用 schema validate 语义，未由 Hibernate 代替迁移修改结构。
5. 应用完成启动后才执行代表性读取。

自动化复验由 `MigrationSmokeTest` 覆盖：legacy V1 文件升级到 V3、当前 V3 文件重启，以及两种状态下的 JPA validate。

## 代表性读取

响应只记录 HTTP 状态、顶层 `data` 数组计数和响应字节数，不保留业务行内容。

| 接口 | HTTP 状态 | `data` 数量 | 响应字节数 | 结果 |
| --- | --- | ---: | ---: | --- |
| `/api/demands` | 200 | 7 | 6112 | 非空，读取成功 |
| `/api/staff` | 200 | 118 | 50348 | 非空，读取成功 |
| `/api/schedules` | 200 | 7 | 2018 | 非空，读取成功 |

## 有界执行与清理

- 后端使用有界验证命令启动，只在迁移、JPA 初始化和三项读取完成期间运行。
- 读取完成后显式停止验证进程，并确认 Task 14 验证监听器不再存在。
- 临时副本位于仓库外，不作为提交内容；未留下持续运行的 Maven、Spring 或浏览器验证进程。
- 未连接或修改任何 MySQL schema；MySQL 8 验证状态仍为待验。
