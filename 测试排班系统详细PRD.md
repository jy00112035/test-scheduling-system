# 测试排班系统详细 PRD

| 项目名称 | 测试排班系统 |
| --- | --- |
| 文档类型 | 详细产品需求文档 |
| 文档版本 | v2.0 |
| 编写日期 | 2026-07-07 |
| 适用范围 | Web 前端、Spring Boot 后端、H2/MySQL 兼容数据模型 |
| 目标读者 | 产品、设计、前端、后端、测试、项目管理 |

---

## 1. 项目概述

### 1.1 项目背景

测试团队在需求评估、人员分配、执行跟踪和投入统计上存在多处断点：

1. 测试需求依赖线下沟通，提交、审批、排期状态不透明。
2. 测试资源分配依赖人工经验，人员是否空闲、是否冲突、是否具备对应测试类型能力难以及时判断。
3. 新员工人力系数、借调、请假、其他任务占用等信息没有统一纳入排期计算。
4. 多产品并行时，资源主管难以快速判断高风险需求、缺口需求和可发布排期。
5. 测试投入统计依赖人工整理，难以沉淀为产品、版本类型、测试类型维度的报表。

本系统目标是建设一套面向测试团队的 Web 排班平台，将“需求提交审批、资源排布、执行看板、人员管理、统计报表、基础配置”沉淀到统一系统中。

### 1.2 产品目标

1. 建立线上化测试需求提交流程，支持项目经理审批和修改后批准。
2. 建立测试资源池，维护人员、角色、测试类型、熟悉模块、保密权限、当前系数和每日可用状态。
3. 建立可视化人力排布工作台，支持按需求批量生成草稿、人工拖拽微调、冲突检测和发布。
4. 建立任务看板，让测试执行人员、测试经理、资源主管和项目经理清楚查看排班。
5. 建立仪表盘和报表，提供并行产品、人力利用率、版本类型投入和产品维度投入统计。
6. 建立字段配置能力，减少版本类型、测试类型、小组、优先级等基础数据变更对代码的依赖。

### 1.3 当前系统技术形态

| 层级 | 技术/实现 |
| --- | --- |
| 前端 | React 18、TypeScript、Ant Design、Recharts、xlsx、Vite |
| 后端 | Spring Boot 3、Spring Security、JWT、Spring Data JPA |
| 数据库 | H2 文件数据库，配置 MySQL 兼容模式 |
| 鉴权 | JWT Bearer Token |
| API 前缀 | `/api` |
| 前端端口 | `3000` |
| 后端端口 | `8080` |

### 1.4 核心用户与职责

| 角色 | 角色标识 | 核心职责 |
| --- | --- | --- |
| 测试经理 | `testManager` | 提交测试需求、管理自己的需求、查看任务和报表、关闭需求 |
| 资源主管 | `resourceManager` | 管理资源池、进行人力排布、审批测试执行人员注册、查看仪表盘和报表 |
| 项目经理 | `projectManager` | 审批测试需求、调整测试周期、参与资源不足协调、查看仪表盘和报表 |
| 测试执行人员 | `testExecutor` | 查看个人或团队任务看板、维护个人信息 |
| 字段管理员 | `fieldAdmin` | 管理基础字段、拥有系统配置和管理能力 |
| 测试组长 | `testLead` | 查看本组人员和任务，维护本组每日可用状态，辅助资源安排 |

---

## 2. 范围说明

### 2.1 本期范围

1. 登录、注册、注册审批、修改密码。
2. 首页数据门户和功能入口。
3. 测试需求提交、编辑、删除、关闭、导出。
4. 测试需求审批、批量批准、批量退回、修改后批准。
5. 人力排布工作台。
6. 任务看板和排班导出。
7. 人员管理、人员批量导入、人员删除。
8. 每日可用状态维护。
9. 仪表盘。
10. 统计报表。
11. 字段配置。
12. 个人中心。

### 2.2 非本期范围

1. 企业 SSO 登录。
2. 外部产品系统同步。
3. 自动通知站内信、邮件或企业微信。
4. 真实 AI 模型推荐；当前推荐以规则算法为主。
5. 多租户隔离。
6. 移动端原生 App。
7. 审计日志和操作追溯后台。

### 2.3 建议补强范围

1. 将人力排布工作台重构为“高风险需求优先 + 批量需求选择 + 匹配人员池 + 两步式生成草稿 + 异常集中发布”。
2. 补充后端冲突检测和推荐排班接口，减少前端承载过重。
3. 将补充测试申请从前端模拟数据升级为后端实体和接口。
4. 将需求、排班、人员操作纳入操作日志。
5. 增加接口级权限控制。

---

## 3. 角色权限矩阵

权限以当前前端 `UserRoleContext` 为准。

| 功能/权限 | 测试经理 | 资源主管 | 项目经理 | 测试执行人员 | 字段管理员 | 测试组长 |
| --- | --- | --- | --- | --- | --- | --- |
| 查看首页 | 是 | 是 | 是 | 是 | 是 | 是 |
| 查看仪表盘 | 是 | 是 | 是 | 否 | 是 | 是 |
| 查看任务看板 | 是 | 是 | 是 | 是 | 是 | 是 |
| 提交测试需求 | 是 | 是 | 是 | 否 | 是 | 是 |
| 查看测试需求 | 是 | 是 | 是 | 否 | 是 | 是 |
| 编辑测试需求 | 是 | 是 | 是 | 否 | 是 | 是 |
| 删除测试需求 | 是 | 是 | 是 | 否 | 是 | 是 |
| 关闭测试需求 | 是 | 是 | 是 | 否 | 是 | 是 |
| 人力排布 | 否 | 是 | 是 | 否 | 是 | 否 |
| 推荐排班 | 否 | 是 | 是 | 否 | 是 | 否 |
| 每日可用状态管理 | 否 | 否 | 否 | 否 | 是 | 是 |
| 人员管理 | 否 | 是 | 是 | 否 | 是 | 是 |
| 注册审批 | 否 | 是 | 是 | 否 | 是 | 否 |
| 测试需求审批 | 否 | 否 | 是 | 否 | 是 | 否 |
| 基础字段配置 | 否 | 否 | 否 | 否 | 是 | 否 |
| 统计报表 | 是 | 是 | 是 | 否 | 是 | 是 |
| 个人中心 | 是 | 是 | 是 | 是 | 是 | 是 |

审批范围规则：

1. 资源主管审批测试执行人员注册。
2. 项目经理审批非测试执行人员注册。
3. 同时具备资源主管和项目经理角色时，可审批全部待审批用户。
4. 项目经理审批测试需求。

---

## 4. 全局交互与状态规则

### 4.1 登录态

1. 登录成功后，后端返回 JWT、用户名、角色列表、展示名、测试类型。
2. 前端将 JWT 存储到 `localStorage.token`。
3. 前端将用户信息存储到 `localStorage.user`。
4. 后续接口请求统一增加请求头：`Authorization: Bearer <token>`。
5. 前端刷新页面时，如果本地存在 token 和 user，则恢复登录态。
6. 退出登录时清除 token 和 user。

### 4.2 API 响应结构

所有后端接口统一返回：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {}
}
```

错误时：

```json
{
  "code": 400,
  "message": "错误原因",
  "data": null
}
```

前端处理规则：

1. `code === 200` 时取 `data`。
2. `code !== 200` 时抛出错误，并用 `message` 提示用户。

### 4.3 日期与时间

1. 后端时区为 GMT+8。
2. 后端日期时间格式为 `yyyy-MM-dd HH:mm:ss`。
3. 排班日期使用 `LocalDate`，格式为 `yyyy-MM-dd`。
4. 测试需求开始和结束日期使用 `LocalDateTime`。
5. 前端展示时可按 `YYYY-MM-DD` 或 `MM/DD` 简化。

### 4.4 通用列表交互

1. 列表页应支持加载态。
2. 删除、关闭、发布等高影响操作必须二次确认。
3. 批量操作必须展示已选数量。
4. 空数据状态需明确说明当前没有数据，而不是空白。
5. 导出操作需要先选择范围，再生成 Excel。

---

## 5. 功能需求

## 5.1 登录与注册

### 5.1.1 登录

入口：未登录时默认进入登录页。

字段：

| 字段 | 类型 | 必填 | 规则 |
| --- | --- | --- | --- |
| 用户名 | 文本 | 是 | 不为空 |
| 密码 | 密码 | 是 | 不为空 |

交互逻辑：

1. 用户输入用户名和密码。
2. 点击登录。
3. 前端调用登录接口。
4. 登录成功后保存 token 和用户信息。
5. 进入系统默认首页。
6. 登录失败提示“用户名或密码错误”“账户已被禁用，请等待管理员审批”等后端错误信息。

默认测试账号：

| 用户名 | 密码 | 角色 |
| --- | --- | --- |
| `admin` | `admin123` | 字段管理员、测试经理 |
| `testmanager` | `test123` | 测试经理 |
| `resourcemanager` | `resource123` | 资源主管 |
| `projectmanager` | `project123` | 项目经理 |
| `testexecutor` | `test123` | 测试执行人员 |
| `testlead` | `test123` | 测试组长 |

### 5.1.2 注册

入口：登录页“立即注册”。

字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| 用户名 | 文本 | 是 | 唯一 |
| 展示名 | 文本 | 是 | 用户展示名称 |
| 密码 | 密码 | 是 | 与确认密码一致 |
| 确认密码 | 密码 | 是 | 与密码一致 |
| 角色 | 单选/多选 | 是 | 注册申请角色 |
| 熟悉模块 | 文本 | 否 | 用于人员匹配 |

交互逻辑：

1. 用户提交注册信息。
2. 后端校验两次密码是否一致。
3. 后端校验用户名是否已存在。
4. 注册成功后用户 `enabled=false`。
5. 用户需等待对应审批人审批。

### 5.1.3 注册审批

入口：审批中心 - 注册审批。

列表字段：

| 字段 | 说明 |
| --- | --- |
| 用户名 | 注册账号 |
| 展示名 | 用户名称 |
| 角色 | 申请角色 |
| 测试类型 | 组长或人员所属测试类型 |
| 熟悉模块 | 能力标签 |
| 申请时间 | 创建时间 |

操作：

1. 单个批准。
2. 单个拒绝。
3. 批量批准。
4. 批量拒绝。

审批规则：

1. 批准后 `enabled=true`。
2. 拒绝后删除注册用户记录。
3. 资源主管只看测试执行人员注册申请。
4. 项目经理看非测试执行人员注册申请。

---

## 5.2 首页

### 5.2.1 页面目标

首页作为登录后的默认入口，帮助用户理解当前身份、可用功能和优先工作入口。

### 5.2.2 页面信息

1. 系统名称和简介。
2. 当前账号。
3. 当前角色数量。
4. 可用功能数量。
5. 推荐优先入口。
6. 工作入口卡片。
7. 角色能力说明。

### 5.2.3 交互逻辑

1. 根据当前角色权限过滤可见入口。
2. 点击入口卡片跳转对应模块。
3. 对无权限模块不展示。
4. 登录后默认选中首页。

---

## 5.3 测试需求管理

### 5.3.1 页面目标

用于提交、查看、编辑、删除、关闭和导出测试需求。

### 5.3.2 需求状态

| 状态值 | 中文含义 | 说明 |
| --- | --- | --- |
| `submitted` | 已提交待审批 | 新建或重新提交后等待项目经理审批 |
| `pending` | 待排期 | 已审批通过，等待资源主管或项目经理排班 |
| `scheduled` | 已排期 | 已有排班或进入排期中 |
| `completed` | 已完成 | 测试需求关闭 |
| `rejected` | 已退回 | 审批退回 |

### 5.3.3 需求字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| 产品 | 文本/配置项 | 是 | `product` |
| 版本号 | 文本 | 否 | `version` |
| 测试开始日期 | 日期 | 是 | `startDate` |
| 测试结束日期 | 日期 | 是 | `endDate` |
| 版本类型 | 下拉 | 是 | `versionType`，来自字段配置 |
| 版本阶段 | 下拉 | 是 | `versionPhase`，来自字段配置 |
| 测试类型人力明细 | 多行输入 | 是 | `manpowerDetails` |
| 总人力需求 | 自动计算 | 是 | 明细求和，`manpowerDemand` |
| 优先级 | 下拉 | 是 | `priority`，默认高/中/低 |
| 是否保密 | 开关 | 否 | `confidential` |
| 样机数量 | 数字 | 否 | `testDeviceCount` |
| 备注说明 | 多行文本 | 否 | `description` |
| 提交人 | 文本 | 否 | `submittedBy` |

测试类型人力明细字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| 测试类型 | 下拉 | 是 | 如功能测试、性能测试 |
| 人力需求 | 数字 | 是 | 单位人天，支持小数 |
| 备注 | 文本 | 否 | 说明特殊要求 |

### 5.3.4 提交流程

1. 用户点击“提交需求”。
2. 填写产品、周期、版本类型、版本阶段、优先级等基础信息。
3. 按测试类型填写人力需求。
4. 系统校验至少一个测试类型的人力需求大于 0。
5. 系统将各测试类型人力相加为总人力需求。
6. 点击提交。
7. 后端保存需求主表和人力明细表。
8. 需求进入 `submitted` 状态，等待项目经理审批。

### 5.3.5 编辑流程

1. 用户在需求列表点击编辑。
2. 系统打开提交需求弹窗并回填数据。
3. 用户修改字段。
4. 保存后调用更新接口。
5. 更新人力明细时，后端先删除旧明细，再写入新明细。

### 5.3.6 删除流程

1. 用户点击删除。
2. 系统二次确认。
3. 后端删除需求。
4. 前端刷新列表。

建议补强：删除需求时应同步删除排班和人力明细，或后端增加外键级联。

### 5.3.7 关闭流程

1. 用户点击关闭。
2. 系统二次确认。
3. 后端将需求状态更新为 `completed`。
4. 已完成需求进入统计口径。

### 5.3.8 导出流程

1. 用户点击导出。
2. 选择日期范围。
3. 系统筛选创建时间或测试周期落在范围内的需求。
4. 生成 Excel 文件。

---

## 5.4 测试需求审批

### 5.4.1 页面目标

项目经理或字段管理员审批 `submitted` 状态的测试需求。

### 5.4.2 列表字段

| 字段 | 说明 |
| --- | --- |
| 产品 | 需求产品 |
| 版本号 | 产品版本 |
| 版本类型 | 维护/在研/升级等 |
| 版本阶段 | 月度维护、紧急版本等 |
| 测试周期 | 开始日期到结束日期 |
| 人力需求 | 总人天 |
| 测试类型明细 | 分测试类型人力 |
| 优先级 | 高/中/低 |
| 是否保密 | 保密需求标识 |
| 样机数量 | 可并行限制依据 |
| 提交人 | 需求提交人 |

### 5.4.3 审批操作

| 操作 | 结果 |
| --- | --- |
| 批准 | 状态从 `submitted` 改为 `pending` |
| 退回 | 状态从 `submitted` 改为 `rejected` |
| 修改并批准 | 修改周期、优先级、样机数、人力明细后，状态改为 `pending` |
| 批量批准 | 对选中需求逐个批准 |
| 批量退回 | 对选中需求逐个退回 |

### 5.4.4 修改并批准规则

1. 只能修改 `submitted` 状态需求。
2. 允许调整测试开始日期、结束日期。
3. 允许调整优先级。
4. 允许调整样机数量。
5. 允许调整测试类型人力明细。
6. 修改后重新计算总人力需求。

---

## 5.5 人力排布工作台

### 5.5.1 页面目标

帮助资源主管和项目经理对已经审批通过的测试需求进行人力排布，并发布可执行排期。

本模块应参考并落地以下设计文档：

`docs/superpowers/specs/2026-07-07-schedule-workbench-redesign-design.md`

最终设计方向为：

**高风险优先 + 批量需求选择 + 匹配人员池 + 两步式生成草稿 + 异常集中发布**。

核心目标：

1. 在大量待排需求和大量测试人员同时存在时，优先帮助用户定位高风险需求。
2. 让用户先看到本批需求是否可排、缺哪些测试类型人力，再决定是否生成草稿。
3. 将推荐排班、人工微调、冲突检测和发布收束到同一条操作链路。
4. 把冲突、缺口、权限不匹配、样机限制等异常集中展示，减少用户在全量矩阵中查找问题。
5. 将关键规则沉到后端校验，避免前端单独承担排班正确性。

### 5.5.2 当前功能结构

当前工作台包含：

1. 顶部操作区：按指定日期排班、按全部需求排班、冲突检测、清除未发布排班。
2. 左侧需求区：待排期需求、已分配项目。
3. 右侧人员日期矩阵：人员行、日期列、单元格任务卡片。
4. 筛选区：测试类型、系数范围、产品。
5. 弹窗：分配任务、编辑排班、按指定日期排班、按全部需求排班、需求详情。
6. 提示区：冲突提示、推荐未满足提示。

当前主要问题：

1. 页面默认展示全量人员矩阵，待排需求和人员数量变多后，用户需要在大范围表格中寻找可用位置。
2. 推荐排班、冲突提示、缺口提示分散，用户难以快速判断本批需求是否可以发布。
3. 前端承担了过多推荐和冲突判断逻辑，后端缺少统一推荐、冲突检测、批量发布和硬校验接口。
4. 人员熟悉模块、保密权限等字段如果不持久化，匹配人员池无法稳定计算。

### 5.5.3 目标页面结构

目标工作台采用“调度指挥台”四区结构：

1. 顶部调度总览条。
2. 左侧需求队列。
3. 中间匹配人员池。
4. 右侧相关时间轴。
5. 最右侧缺口与发布面板。

页面默认不展示全量人员矩阵，而是先展示待处理需求。用户选择一批需求后，系统根据所选需求自动收束人员和日期范围，再展示相关人员、容量、草稿和异常。

#### 5.5.3.1 顶部调度总览条

顶部总览条固定在工作台顶部，展示当前排布上下文和主操作。

展示指标：

1. 待排需求数量。
2. 高风险需求数量。
3. 本批已选需求数量。
4. 预计缺口人天。
5. 草稿排班数量。
6. 可发布需求数量。

主操作：

1. 生成推荐草稿。
2. 只看异常。
3. 清除本批草稿。
4. 发布本批。

按钮状态：

1. 未选择需求时，`生成推荐草稿` 禁用。
2. 选择需求后，先展示可行性预估，`生成推荐草稿` 可用。
3. 草稿生成后，`发布本批` 可用。
4. 存在冲突或缺口的需求默认不参与一键发布。

#### 5.5.3.2 左侧需求队列

需求队列负责帮助用户决定“先排哪些需求”。

默认排序：

1. 已超过或临近测试结束日期的需求。
2. 剩余人天缺口大的需求。
3. 优先级高的需求。
4. 保密需求或特殊测试类型需求。
5. 已有草稿但未发布的需求。

筛选项：

1. 测试类型。
2. 优先级。
3. 产品。
4. 版本类型。
5. 截止时间。
6. 是否保密。
7. 是否已有草稿。
8. 是否存在缺口。

需求卡片展示：

1. 产品和版本。
2. 优先级。
3. 测试周期。
4. 总人力需求。
5. 已发布人天。
6. 草稿人天。
7. 剩余人天。
8. 测试类型明细。
9. 保密标识。
10. 样机数量。
11. 风险标签。

交互规则：

1. 支持单选、批量勾选、全选当前筛选结果。
2. 点击需求卡片打开需求详情抽屉。
3. 勾选需求后，匹配人员池、相关时间轴、缺口与发布面板立即刷新。
4. 对已经完全发布且未过期的需求，展示在“已分配”分组，不进入默认待排队列。

#### 5.5.3.3 中间匹配人员池

匹配人员池负责回答“哪些人适合当前这批需求”。

人员排序依据：

1. 测试类型匹配度。
2. 熟悉模块匹配度。
3. 保密权限匹配。
4. 当前人力系数。
5. 目标日期范围内可用率。
6. 已有排班负载。
7. 是否存在冲突。

人员卡片展示：

1. 姓名。
2. 测试类型。
3. 当前系数。
4. 熟悉模块。
5. 保密权限。
6. 本批日期范围内可用率。
7. 当前冲突状态。
8. 推荐匹配分。

交互规则：

1. 默认只展示与本批需求相关的人员。
2. 支持切换“全部人员”，用于人工兜底。
3. 点击人员卡片时，右侧时间轴聚焦该人员。
4. 可将人员固定到本批，生成草稿时优先使用固定人员。
5. 可将人员排除出本批，生成草稿时不使用该人员。

#### 5.5.3.4 右侧相关时间轴

相关时间轴负责展示本批需求与匹配人员在日期维度上的排布关系。

默认展示范围：

1. 已选择需求时，展示本批需求的最早开始日期到最晚结束日期，默认最多展示两周。
2. 如果跨越超过两周，支持周切换和日期范围缩放。
3. 未选择需求时，展示本周和下周的容量概览。

时间轴行：

1. 匹配人员。
2. 已固定人员。
3. 存在冲突但与本批相关的人员。
4. 缺口行。

单元格展示：

1. 已发布排班。
2. 草稿排班。
3. 非空闲状态，例如请假、借调、其他任务。
4. 总负载百分比。
5. 冲突状态。

交互规则：

1. 生成草稿前，时间轴展示容量、已有排班和可用空位。
2. 生成草稿后，时间轴展示推荐草稿，用户可以拖拽移动、修改百分比、删除草稿。
3. 已发布排班默认锁定，修改后进入本地“待提交变更”状态。
4. 冲突单元格高亮显示，并在缺口与发布面板同步列出原因。

#### 5.5.3.5 缺口与发布面板

缺口与发布面板负责集中展示本批需求的可行性和异常。

生成草稿前展示：

1. 本批需求总人天。
2. 预计可满足人天。
3. 预计缺口人天。
4. 缺口测试类型。
5. 缺口原因。
6. 建议动作。

生成草稿后展示：

1. 已生成草稿数量。
2. 无冲突可发布需求数量。
3. 有冲突需求数量。
4. 未满足需求数量。
5. 每个异常需求的原因和建议动作。

异常类型：

1. 单人单日负载超过当前容量。
2. 新员工系数不足。
3. 保密需求缺少可用保密人员。
4. 指定测试类型没有可用人员。
5. 样机数量限制导致无法并行分配。
6. 需求周期过短导致人天无法满足。
7. 已发布排班占用过高。

建议动作：

1. 延长测试周期。
2. 调整需求优先级。
3. 排除冲突人员。
4. 固定特定人员后重新生成。
5. 触发项目经理协调。
6. 保留为草稿暂不发布。

### 5.5.4 标准操作流程

#### 5.5.4.1 高风险需求优先排布

1. 高风险需求默认排在最前。
2. 用户批量选择本轮要排的需求。
3. 系统展示匹配人员和缺口预估。
4. 用户点击生成推荐草稿。
5. 用户在时间轴微调草稿。
6. 系统集中展示冲突和缺口。
7. 用户发布无冲突且满足人力的需求。

#### 5.5.4.2 按测试类型批量排布

1. 用户在需求队列选择测试类型筛选条件。
2. 系统只展示包含该测试类型人力需求的需求。
3. 用户全选当前筛选结果或手动勾选部分需求。
4. 匹配人员池优先展示该测试类型人员。
5. 用户生成推荐草稿。
6. 用户处理冲突并发布。

#### 5.5.4.3 处理自动排布缺口

1. 系统生成草稿后发现部分需求未满足。
2. 缺口与发布面板展示缺口需求、缺口人天、缺口测试类型和原因。
3. 用户选择建议动作。
4. 如果选择延长周期，系统提示需要项目经理协调。
5. 如果选择固定人员，用户在人员池固定目标人员后重新生成草稿。
6. 如果选择保留草稿，系统允许发布其他无冲突需求。

### 5.5.5 待排需求规则

待排需求来源：

1. 状态为 `pending` 的需求。
2. 状态为 `scheduled` 但仍有剩余人力缺口的需求。

剩余人力计算：

```
剩余人力 = 需求总人力 - 已发布排班人天
排班人天 = sum(percentage / 100)
```

风险排序计算：

1. 剩余天数越少，风险越高。
2. 剩余人天越多，风险越高。
3. 优先级越高，风险越高。
4. 保密需求没有足够保密人员时，风险升高。
5. 特定测试类型没有可用人员时，风险升高。
6. 已有草稿未发布时，单独标记但不一定提高风险。

### 5.5.6 两步式生成草稿

“两步式”指两个有明确边界的交互阶段。

#### 第一步：可行性预估

可行性预估为轻量、即时、不写入数据库的阶段。

触发条件：

1. 用户勾选一批需求。
2. 用户调整固定人员、排除人员或日期范围。
3. 用户切换是否包含周六、周日。

前端计算内容：

1. 本批需求总人天，按测试类型拆分。
2. 匹配人员池在日期范围内的可用容量。
3. 预计可满足人天和预计缺口人天。
4. 缺口按测试类型列出原因。

边界：

1. 不调用后端推荐算法。
2. 不生成任何 `schedule` 记录。
3. 不清理已有草稿。
4. 前端只做容量加减，不做具体“人员-日期-百分比”分配。

#### 第二步：生成推荐草稿

生成推荐草稿为调用后端推荐算法、写入数据库的阶段。

触发条件：

1. 用户已选择一批需求。
2. 用户已查看可行性预估。
3. 用户点击 `生成推荐草稿` 并确认。

后端行为：

1. 接收需求 ID、日期范围、固定人员、排除人员、周末策略。
2. 清理当前用户在本批需求下的旧草稿。
3. 运行推荐算法。
4. 创建具体 `schedule` 记录，并设置 `published=false`。
5. 返回已生成草稿、未满足需求、冲突列表和缺口原因。

前端行为：

1. 时间轴从容量概览切换为草稿排班视图。
2. 缺口与发布面板从预估结果切换为实际结果。
3. 用户可以拖拽、编辑、删除草稿。
4. 用户可对人员池重新固定或排除人员后再次生成。

### 5.5.7 人工分配流程

1. 用户从左侧选择或拖拽一个需求。
2. 用户在人员日期矩阵中选择目标人员和日期。
3. 系统打开分配弹窗。
4. 用户填写分配百分比和连续分配天数。
5. 系统校验本次分配是否超过剩余需求。
6. 系统按连续天数批量创建排班草稿。
7. 前端刷新矩阵。

分配校验：

1. 未选择需求时不能分配。
2. 需求人力已满足时提示无需继续分配。
3. 本次分配人天不能超过剩余人天。
4. 非空闲或不可用人员应提示不可分配。
5. 如果人员单日负载超过容量，应标记冲突。

硬阻断校验：

1. 单人单日累计排班百分比超过 `currentCoefficient * 100` 时，前端直接阻止提交，后端拒绝保存。
2. 保密需求分配给无保密权限人员时，前端拒绝拖拽或提交，后端拒绝保存。
3. 已发布排班不得直接删除，必须先取消发布。
4. 样机数量超限首版允许作为软警告，第二版升级为硬阻断。

### 5.5.8 推荐排班算法约束

推荐排班入口保留两种业务模式，但统一走“可行性预估 -> 生成草稿”链路：

1. 按指定日期排班。
2. 按全部需求排班。

按指定日期排班：

1. 用户打开弹窗。
2. 勾选需求。
3. 勾选排班日期。
4. 选择是否包含周六、周日。
5. 点击生成推荐。
6. 系统生成未发布草稿。

按全部需求排班：

1. 用户打开弹窗。
2. 勾选需求。
3. 选择是否包含周六、周日。
4. 系统从需求最早开始日期起计算，最多覆盖 90 天。
5. 持续尝试分配直到需求满足或无可用资源。

推荐算法约束：

1. 已发布排班不被覆盖。
2. 非选中需求的排班不被覆盖。
3. 选中需求的未发布旧草稿会被清理后重建。
4. 员工状态为非空闲时按百分比扣减容量。
5. 新员工当前系数决定单日最大容量。
6. 保密需求只能分配给具备保密权限的人员。
7. 测试类型需求优先分配给对应测试类型人员。
8. 样机数量限制同一需求同一天并行人数。
9. 需求开始日期之前不可排班。

### 5.5.9 状态模型

#### 需求排布状态

1. `未选择`：需求在队列中，但不参与当前批次。
2. `本批已选`：需求参与当前批次，会影响匹配人员和缺口预估。
3. `预估可满足`：尚未生成草稿，但容量预估可以满足。
4. `预估有缺口`：尚未生成草稿，容量预估存在缺口。
5. `草稿已生成`：已生成未发布排班。
6. `草稿有冲突`：草稿中存在负载、权限或规则冲突。
7. `可发布`：草稿无冲突且人力满足。
8. `已发布`：排班已发布。

#### 排班卡片状态

1. `草稿`：`published=false`，可拖拽、可编辑、可删除。
2. `已发布`：`published=true`，默认锁定。
3. `待提交变更`：已发布排班被修改后产生的前端本地状态，后端仍为 `published=true`。
4. `冲突`：超容量、权限不匹配、周期不合法或样机限制超限。

#### 人员状态

1. `推荐`：匹配分高且可用率足够。
2. `可用`：符合基础条件但不是最优。
3. `低可用`：可用率不足或系数较低。
4. `冲突`：当前周期内已经超过容量。
5. `不可用`：权限、测试类型或状态不满足本批需求。

状态约束：

| 约束 | 说明 |
| --- | --- |
| 草稿 -> 已发布 | 只有 `published=false` 的排班可以被发布 |
| 已发布 -> 待提交变更 | 仅前端本地状态，刷新页面后丢失，需 localStorage 防护 |
| 草稿 -> 删除 | 草稿可直接删除 |
| 已发布 -> 删除 | 必须先取消发布，不允许直接删除 |
| 已发布 -> 拖拽转移 | 实际是修改 `staffId` 或 `date`，进入待提交变更状态 |

### 5.5.10 冲突检测规则

| 冲突类型 | 判定规则 | 提示方式 |
| --- | --- | --- |
| 单日超负载 | 某人员某天排班百分比总和 > 人员系数和每日状态折算容量 | 单元格红色，高亮冲突列表 |
| 新员工系数不足 | 分配比例大于当前系数容量 | 标记超出容量 |
| 请假/借调/其他任务 | 每日状态非空闲且剩余容量不足 | 单元格状态卡片 |
| 保密权限不足 | 保密需求分配给无保密权限人员 | 阻止或标记冲突 |
| 测试类型不匹配 | 需求测试类型无法找到对应人员 | 未满足提示 |
| 样机并行限制 | 同一需求同一天人员数超过样机数量 | 未满足或冲突提示 |

前后端配合要求：

1. 前端负责在拖拽、分配、编辑时即时阻止明显冲突。
2. 后端负责在创建、批量创建、更新、发布时再次校验，前端校验不能替代后端校验。
3. 后端冲突检测结果应返回具体 `demandId`、`staffId`、日期、原因和建议动作。

### 5.5.11 发布规则

1. 排班创建后默认 `published=false`。
2. 用户可按需求发布。
3. 发布时将该需求下所有排班记录设置为 `published=true`。
4. 已发布排班可取消发布。
5. 发布前应建议进行冲突检测。
6. 一键发布默认跳过有冲突或未满足的需求。
7. 不允许强制发布缺口需求。
8. 发布前二次确认，确认内容包含需求数量、排班条数、异常跳过数量。
9. 批量发布采用部分成功/失败模式：可发布需求成功发布，有问题需求保留草稿并返回失败原因。

### 5.5.12 清除规则

1. 清除全部未发布排班：删除所有 `published=false` 的排班记录。
2. 清除指定需求排班：默认只删除指定需求下当前用户的未发布草稿。
3. 删除单条排班：删除指定排班记录。
4. 对已发布排班做本地改动时，应标记为待提交变更。
5. 后端 `DELETE /schedules/demand/{demandId}` 默认不得删除已发布排班。

### 5.5.13 草稿持久化与离开防护

前端应对以下未保存状态进行保护：

1. 已选但未生成草稿的需求。
2. 固定人员和排除人员配置。
3. 可行性预估结果。
4. 对已发布排班产生的待提交变更。
5. 已生成但未发布的草稿编辑上下文。

实现要求：

1. 页面存在未保存状态时，刷新或关闭页面需要 `beforeunload` 拦截。
2. 将关键 UI 状态以当前登录用户为维度写入 `localStorage`。
3. 状态变化后 2 秒内自动保存，避免频繁写入。
4. 页面重新进入时，如果检测到未完成排布操作，弹窗询问是否恢复。
5. 用户成功发布或主动丢弃后，清除对应本地草稿。
6. 第一阶段不新增后端草稿表；如果后续需要多设备同步，再评估后端草稿表。

### 5.5.14 并发与数据一致性

并发风险：

| 场景 | 风险 | 处理要求 |
| --- | --- | --- |
| 两位资源主管同时对同一需求生成推荐草稿 | 后生成的覆盖先生成的草稿 | 草稿按 `createdBy` 隔离 |
| 用户 A 微调草稿时用户 B 发布同一需求 | 用户 A 基于过期数据编辑 | 发布时做乐观锁或更新时间校验 |
| 用户点击发布时另一用户已先发布 | 脏写 | 返回 409 冲突，提示刷新 |

数据一致性要求：

1. 草稿记录增加 `createdBy` 字段，推荐草稿查询和清理按 `demandId + createdBy` 过滤。
2. 其他用户草稿不计入当前用户容量计算；容量计算以已发布排班为准，当前用户自己的草稿可在前端叠加展示。
3. `schedule` 表增加 `version` 或 `updated_at`，用于发布和修改时的乐观校验。
4. 发布失败时必须保留原草稿，不得静默丢失用户调整。

### 5.5.15 错误处理与边界场景

| 场景 | 处理策略 |
| --- | --- |
| 生成推荐草稿接口超时或失败 | 保留需求勾选和人员池配置，提示失败原因，提供重试和放弃 |
| 生成推荐草稿部分成功 | 展示已生成草稿，同时在缺口面板列出未满足需求 |
| 发布本批部分成功 | 展示成功数量、失败数量和逐项失败原因 |
| 数据加载失败 | 各区域独立展示重试按钮，不影响其他已加载区域 |
| 待排需求为空 | 展示“所有需求已排布完成”，可跳转已分配分组 |
| 筛选结果为空 | 提供“清除筛选”操作 |
| 匹配人员为空 | 提示切换全部人员或调整需求选择 |
| 后端推荐超过 10 秒 | 后续可升级为异步任务，返回 `taskId` 并由前端轮询 |

### 5.5.16 分阶段实施建议

第一阶段：布局重构 + 风险排序 + 集中异常。

1. 拆分 `ScheduleWorkbench.tsx`。
2. 增加顶部调度总览条、需求队列、缺口与发布面板。
3. 保持现有创建、编辑、发布逻辑不变。
4. 增加 localStorage 草稿保护。

第二阶段：匹配人员池 + 新推荐排班 API。

1. 持久化 `familiarModules` 和 `confidentialClearance`。
2. 实现匹配人员池和匹配分。
3. 增加 `POST /schedules/recommend/draft`。
4. 增加 `GET /staff/availability`。
5. 后端增加累计百分比硬阻断。

第三阶段：批量发布 + 异步推荐 + 性能优化。

1. 增加批量发布接口，采用部分成功/失败模式。
2. 增加推荐排班异步任务能力。
3. 改造按需求删除排班接口，保护已发布排班。
4. 引入时间轴虚拟滚动和并发乐观锁。

---

## 5.6 任务看板

### 5.6.1 页面目标

展示已发布排班和每日可用状态，让用户清楚知道人员每天承担什么任务。

### 5.6.2 数据来源

1. 活跃人员：`GET /api/staff/active`。
2. 已发布排班：`GET /api/schedules/published`。
3. 每日状态：`GET /api/daily-statuses?startDate=&endDate=`。

### 5.6.3 筛选与视图

1. 默认按当前周展示。
2. 支持切换周。
3. 支持小组筛选。
4. 支持产品筛选。
5. 支持导出指定日期范围排班。

### 5.6.4 单元格展示

1. 空闲状态。
2. 请假、借调、其他任务、调休等每日状态。
3. 一个或多个排班卡片。
4. 每张排班卡片展示产品、版本、版本类型、投入百分比、测试经理。

### 5.6.5 导出规则

1. 用户选择日期范围。
2. 可选产品和版本过滤。
3. 导出 Excel。

---

## 5.7 人员管理

### 5.7.1 页面目标

维护测试人员资源池和对应登录账号。

### 5.7.2 人员字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| 姓名 | 文本 | 是 | `name` |
| 工号 | 文本 | 是 | `empNo`，唯一；同时作为用户账号 |
| 入职日期 | 日期 | 是 | `joinDate` |
| 所属小组 | 下拉 | 是 | `groupName` |
| 测试类型 | 下拉 | 否 | `testType` |
| 初始系数 | 数字 | 是 | `initialCoefficient` |
| 当前系数 | 数字 | 是 | `currentCoefficient` |
| 状态 | 下拉 | 是 | active、leave、resigned |
| 角色 | 单选/多选 | 是 | 用户角色 |
| 熟悉模块 | 文本 | 否 | `familiarModules` |
| 保密权限 | 开关 | 否 | `confidentialClearance` |

### 5.7.3 新增人员

1. 用户填写人员信息。
2. 系统创建 `test_staff` 记录。
3. 系统同步创建 `users` 账号。
4. 用户名为工号。
5. 默认密码为 `12345678`。
6. 创建成功后返回生成密码。

### 5.7.4 编辑人员

1. 用户点击编辑。
2. 系统打开弹窗并回填人员信息。
3. 保存后更新 `test_staff`。
4. 如果工号变更，同步修改 `users.username`。
5. 同步更新角色、熟悉模块、保密权限。

### 5.7.5 删除人员

1. 单个删除需要二次确认。
2. 批量删除需要选中人员并二次确认。
3. 删除人员时同步删除对应用户账号。
4. 测试组长不能删除人员。
5. 测试组长只能编辑本测试类型人员。

### 5.7.6 批量导入

1. 用户上传 Excel。
2. 前端解析 Excel。
3. 系统预览导入数据。
4. 检测重复工号。
5. 用户确认导入。
6. 前端逐条调用创建人员接口。

建议补强：后端提供批量导入接口，统一校验并事务提交。

---

## 5.8 每日可用状态

### 5.8.1 页面目标

让测试组长或字段管理员维护人员每日可用情况，供排班算法扣减容量。

### 5.8.2 状态枚举

| 状态值 | 中文 | 含义 |
| --- | --- | --- |
| `AVAILABLE` | 空闲 | 默认可用 |
| `OTHER_TASKS` | 完成其他任务 | 当日部分或全部被其他任务占用 |
| `SECONDED` | 借调其他项目组 | 当日被借调 |
| `ON_LEAVE` | 请假 | 当日请假 |
| `COMPENSATORY_LEAVE` | 调休 | 当日调休 |

### 5.8.3 交互逻辑

1. 用户在任务看板或排布工作台点击人员日期单元格。
2. 弹出状态设置浮层。
3. 选择状态。
4. 如果状态为 `AVAILABLE`，默认百分比为 100。
5. 如果状态非 `AVAILABLE`，填写占用百分比。
6. 保存后写入或更新 `staff_daily_status`。

### 5.8.4 计算规则

```
人员原始容量 = currentCoefficient * 100
状态占用 = percentage
剩余容量 = 原始容量 * (1 - 状态占用 / 100)
```

如果状态为 `AVAILABLE`，剩余容量按原始容量计算。

---

## 5.9 仪表盘

### 5.9.1 页面目标

为管理角色提供测试资源与需求的实时概览。

### 5.9.2 指标

| 指标 | 计算口径 |
| --- | --- |
| 当前并行测试产品数 | 今日有已发布排班的产品去重数量 |
| 测试执行总人数 | 活跃人员数量 |
| 今日投入测试人数 | 今日有已发布排班的人员去重数量 |
| 今日空闲人数 | 活跃人员 - 今日投入人员 |
| 人力利用率 | 今日已投入人员数 / 活跃人员数 |
| 测试需求总数 | 需求总数 |

### 5.9.3 图表

1. 测试类型资源看板。
2. 并行测试数量趋势，当前实现为近 7 天。
3. 人力利用率走势，当前实现为近 7 天。
4. 各版本类型人力投入占比。
5. 测试需求统计。

---

## 5.10 统计报表

### 5.10.1 页面目标

按产品维度统计计划人力、实际投入、人力差异和平均周期。

### 5.10.2 筛选

| 筛选项 | 说明 |
| --- | --- |
| 时间周期 | 月度、季度、年度 |

### 5.10.3 报表字段

| 字段 | 计算口径 |
| --- | --- |
| 产品 | 按产品分组 |
| 测试需求数 | 该产品需求数量 |
| 计划人力总需求 | 需求 `manpowerDemand` 求和 |
| 实际人力总投入 | 已发布排班 `percentage / 100` 求和 |
| 人力差异率 | `(实际 - 计划) / 计划` |
| 平均测试周期 | 需求周期天数平均值 |

---

## 5.11 审批中心

### 5.11.1 页面目标

统一承载注册审批和测试需求审批。

### 5.11.2 入口规则

1. 如果有注册审批权限，显示注册审批 Tab。
2. 如果有测试需求审批权限，显示需求审批 Tab。
3. 侧边栏审批中心显示待审批数量徽标。
4. 进入审批中心时刷新待审批数量。
5. 每 30 秒自动刷新待审批数量。

---

## 5.12 基础字段配置

### 5.12.1 页面目标

字段管理员维护系统基础字段，减少硬编码配置。

### 5.12.2 字段配置结构

| 字段 | 说明 |
| --- | --- |
| `fieldName` | 字段名 |
| `fieldType` | 字段类型，如 input、select |
| `options` | 选项，逗号分隔 |
| `description` | 描述 |
| `required` | 是否必填 |
| `sortOrder` | 排序 |

### 5.12.3 初始化配置

| 字段名 | 类型 | 默认选项 |
| --- | --- | --- |
| `versionType` | select | 维护,在研,升级 |
| `versionPhase` | select | 月度维护,紧急版本,第一版,第二版,第三版,第四版,第五版,Tag线版本,合格版本,合同期维护版本 |
| `productName` | input | 空 |
| `testType` | select | 功能测试,自动化测试,性能测试,安全测试,兼容性测试 |
| `groupName` | select | 功能测试组,自动化测试组,性能测试组 |
| `priority` | select | 高,中,低 |

### 5.12.4 交互逻辑

1. 新增字段配置。
2. 编辑字段配置。
3. 删除字段配置。
4. 列表展示字段名、类型、选项、描述、必填、排序。

---

## 5.13 个人中心

### 5.13.1 页面目标

展示当前用户信息，并支持修改密码。

### 5.13.2 修改密码字段

| 字段 | 必填 | 规则 |
| --- | --- | --- |
| 原密码 | 是 | 必须匹配当前密码 |
| 新密码 | 是 | 与确认密码一致 |
| 确认密码 | 是 | 与新密码一致 |

交互逻辑：

1. 用户输入原密码、新密码、确认密码。
2. 前端提交修改密码接口。
3. 后端校验登录态、原密码和两次新密码。
4. 成功后提示密码修改成功。

---

## 5.14 补充测试申请

### 5.14.1 页面目标

用于在测试执行过程中发现人力不足时提交补充测试申请。

### 5.14.2 当前实现状态

当前前端存在补充测试申请页面，但数据为前端模拟数据，后端尚无对应实体和接口。

### 5.14.3 建议字段

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| 原需求 ID | 数字 | 是 | 关联测试需求 |
| 产品 | 只读 | 是 | 来源原需求 |
| 原计划人力 | 数字 | 是 | 来源原需求 |
| 当前已投入人力 | 数字 | 是 | 来源排班统计 |
| 补充原因 | 多行文本 | 是 | 人力不足原因 |
| 补充结束日期 | 日期 | 是 | 新结束日期 |
| 补充人力需求 | 数字 | 是 | 单位人天 |
| 状态 | 枚举 | 是 | submitted、approved、rejected、changed_approved |
| 审批人 | 文本 | 否 | 项目经理 |
| 驳回原因 | 文本 | 否 | 审批驳回时填写 |

### 5.14.4 建议流程

1. 测试经理或资源主管选择一个未完成需求。
2. 系统展示原需求和当前投入。
3. 用户填写补充原因、补充结束日期、补充人力。
4. 提交后进入项目经理审批。
5. 项目经理可同意、驳回、修改后同意。
6. 审批通过后更新需求周期或产生新的补充排班任务。

---

## 6. 接口需求

### 6.1 通用说明

Base URL：

```text
http://<host>:8080/api
```

请求头：

```text
Content-Type: application/json
Authorization: Bearer <JWT>
```

### 6.2 认证接口

| 方法 | 路径 | 描述 | 请求体 | 返回 |
| --- | --- | --- | --- | --- |
| POST | `/auth/login` | 登录 | `{ username, password }` | `{ token, username, role, roles, displayName, testType }` |
| POST | `/auth/change-password` | 修改密码 | `{ oldPassword, newPassword, confirmPassword }` | 空 |
| POST | `/auth/register` | 注册 | `{ username, password, confirmPassword, displayName, role, roles?, familiarModules? }` | 空 |
| GET | `/auth/pending-approvals` | 获取待审批注册用户 | 无 | `User[]` |
| PUT | `/auth/approve/{id}` | 批准用户 | 无 | 空 |
| PUT | `/auth/reject/{id}` | 拒绝用户 | 无 | 空 |
| PUT | `/auth/batch-approve` | 批量批准用户 | `number[]` | 空 |
| PUT | `/auth/batch-reject` | 批量拒绝用户 | `number[]` | 空 |

### 6.3 测试需求接口

| 方法 | 路径 | 描述 | 请求体/参数 | 返回 |
| --- | --- | --- | --- | --- |
| GET | `/demands` | 获取全部需求 | 无 | `TestDemand[]` |
| GET | `/demands/{id}` | 获取需求详情 | id | `TestDemand` |
| GET | `/demands/pending` | 获取待排期和已排期需求 | 无 | `TestDemand[]` |
| POST | `/demands` | 创建需求 | `TestDemand` | `TestDemand` |
| PUT | `/demands/{id}` | 更新需求 | `TestDemand` | `TestDemand` |
| DELETE | `/demands/{id}` | 删除需求 | id | 空 |
| POST | `/demands/{id}/close` | 关闭需求 | id | `TestDemand` |
| GET | `/demands/pending-approval` | 获取待审批需求 | 无 | `TestDemand[]` |
| PUT | `/demands/{id}/approve` | 批准需求 | id | `TestDemand` |
| PUT | `/demands/{id}/reject` | 退回需求 | id | 空 |
| PUT | `/demands/{id}/approve-with-changes` | 修改并批准 | `TestDemand` | `TestDemand` |
| PUT | `/demands/{id}/priority` | 更新优先级 | `{ priority }` | `TestDemand` |
| PUT | `/demands/batch-approve` | 批量批准需求 | `number[]` | 空 |
| PUT | `/demands/batch-reject` | 批量退回需求 | `number[]` | 空 |

### 6.4 排班接口

| 方法 | 路径 | 描述 | 请求体/参数 | 返回 |
| --- | --- | --- | --- | --- |
| GET | `/schedules` | 获取全部排班 | 无 | `Schedule[]` |
| GET | `/schedules/{id}` | 获取排班详情 | id | `Schedule` |
| GET | `/schedules/date/{date}` | 按日期获取排班 | `yyyy-MM-dd` | `Schedule[]` |
| GET | `/schedules/range` | 按日期范围获取排班 | `startDate`, `endDate` | `Schedule[]` |
| GET | `/schedules/staff/{staffId}` | 按人员获取排班 | staffId | `Schedule[]` |
| GET | `/schedules/published` | 获取已发布排班 | 无 | `Schedule[]` |
| POST | `/schedules` | 创建排班 | `Schedule` | `Schedule` |
| POST | `/schedules/batch` | 批量创建排班 | `Schedule[]` | `Schedule[]` |
| PUT | `/schedules/{id}` | 更新排班日期和百分比 | `Schedule` | `Schedule` |
| DELETE | `/schedules/{id}` | 删除单条排班 | id | 空 |
| DELETE | `/schedules/demand/{demandId}` | 删除某需求全部排班 | demandId | 空 |
| PUT | `/schedules/publish/{demandId}` | 发布某需求排班 | demandId | 空 |
| PUT | `/schedules/unpublish/{demandId}` | 取消发布某需求排班 | demandId | 空 |

### 6.5 人员接口

| 方法 | 路径 | 描述 | 请求体/参数 | 返回 |
| --- | --- | --- | --- | --- |
| GET | `/staff` | 获取全部人员 | 无 | `TestStaff[]` |
| GET | `/staff/active` | 获取活跃人员 | 无 | `TestStaff[]` |
| GET | `/staff/group/{groupName}` | 按小组获取人员 | groupName | `TestStaff[]` |
| GET | `/staff/{id}` | 获取人员详情 | id | `TestStaff` |
| GET | `/staff/role/{empNo}` | 获取工号主角色 | empNo | `string` |
| GET | `/staff/roles/{empNo}` | 获取工号角色列表 | empNo | `string[]` |
| POST | `/staff` | 创建人员并创建账号 | `StaffRequest` | `{ staff, generatedPassword }` |
| PUT | `/staff/{id}` | 更新人员和账号 | `StaffRequest` | `TestStaff` |
| DELETE | `/staff/{id}` | 删除人员和账号 | id | 空 |
| DELETE | `/staff/batch` | 批量删除人员和账号 | `number[]` | 空 |

### 6.6 每日状态接口

| 方法 | 路径 | 描述 | 请求体/参数 | 返回 |
| --- | --- | --- | --- | --- |
| GET | `/daily-statuses` | 获取日期范围内每日状态 | `startDate`, `endDate` | `StaffDailyStatus[]` |
| PUT | `/daily-statuses` | 设置每日状态 | `{ staffId, date, status, percentage }` | 空 |

### 6.7 字段配置接口

| 方法 | 路径 | 描述 | 请求体/参数 | 返回 |
| --- | --- | --- | --- | --- |
| GET | `/field-configs` | 获取全部字段配置 | 无 | `FieldConfig[]` |
| GET | `/field-configs/{id}` | 获取配置详情 | id | `FieldConfig` |
| GET | `/field-configs/name/{fieldName}` | 按字段名获取配置 | fieldName | `FieldConfig` |
| POST | `/field-configs` | 创建字段配置 | `FieldConfig` | `FieldConfig` |
| PUT | `/field-configs/{id}` | 更新字段配置 | `FieldConfig` | `FieldConfig` |
| DELETE | `/field-configs/{id}` | 删除字段配置 | id | 空 |

### 6.8 建议新增接口

#### 6.8.1 人力排布工作台新增接口

这些接口用于支撑“调度指挥台 + 两步式生成草稿”设计，属于前后端都需要配合新增或改造的核心接口。

##### 6.8.1.1 可行性预估接口

第一阶段可以由前端直接聚合计算；当数据量变大或规则复杂后，应由后端提供统一预估接口。

| 项 | 内容 |
| --- | --- |
| 方法 | `POST` |
| 路径 | `/schedules/recommend/preview` |
| 用途 | 对一批需求进行可行性预估，不写入数据库 |
| 权限 | `resourceManager`、`projectManager` |

请求体：

```json
{
  "demandIds": [101, 102],
  "dateRange": {
    "startDate": "2026-07-08",
    "endDate": "2026-07-21"
  },
  "fixedStaffIds": [1, 2],
  "excludedStaffIds": [8, 9],
  "includeSaturdays": false,
  "includeSundays": false
}
```

返回：

```json
{
  "totalManpower": 20.0,
  "estimatedCapacity": 18.5,
  "estimatedShortage": 1.5,
  "shortageByTestType": [
    {
      "testType": "功能测试",
      "required": 8.0,
      "available": 6.5,
      "shortage": 1.5,
      "reasons": ["日期范围内功能测试人员容量不足"]
    }
  ],
  "matchedStaff": [
    {
      "staffId": 1,
      "staffName": "张三",
      "testType": "功能测试",
      "matchScore": 86,
      "availableManpower": 5.0,
      "confidentialClearance": true,
      "matchReasons": ["测试类型匹配", "熟悉模块命中", "可用率高"]
    }
  ],
  "warnings": [
    {
      "type": "DEVICE_LIMIT",
      "message": "部分需求样机数量较少，生成草稿后可能出现并行限制"
    }
  ]
}
```

规则：

1. 该接口不得创建、更新或删除 `schedule` 记录。
2. 返回结果仅用于辅助决策，不等同于最终排班方案。
3. 前端在无该接口时，可基于已有需求、人员、排班、每日状态数据先行计算。

##### 6.8.1.2 生成推荐草稿接口

| 项 | 内容 |
| --- | --- |
| 方法 | `POST` |
| 路径 | `/schedules/recommend/draft` |
| 用途 | 根据本批需求和人员池配置生成未发布草稿 |
| 权限 | `resourceManager`、`projectManager` |

请求体：

```json
{
  "demandIds": [101, 102],
  "dateRange": {
    "startDate": "2026-07-08",
    "endDate": "2026-07-21"
  },
  "fixedStaffIds": [1, 2],
  "excludedStaffIds": [8, 9],
  "includeSaturdays": false,
  "includeSundays": false,
  "replaceCurrentUserDrafts": true
}
```

返回：

```json
{
  "batchId": "WB-20260708-0001",
  "generatedSchedules": [
    {
      "id": 9001,
      "demandId": 101,
      "staffId": 1,
      "date": "2026-07-08",
      "percentage": 100,
      "published": false
    }
  ],
  "fulfilledDemands": [
    {
      "demandId": 101,
      "generatedManpower": 8.0
    }
  ],
  "unfulfilledDemands": [
    {
      "demandId": 102,
      "product": "产品 A",
      "totalManpower": 12.0,
      "fulfilledManpower": 10.5,
      "shortage": 1.5,
      "shortageByTestType": [
        {
          "testType": "性能测试",
          "shortage": 1.5
        }
      ],
      "reasons": ["性能测试人员在目标周期内容量不足"]
    }
  ],
  "conflicts": []
}
```

后端处理要求：

1. 默认只清理当前登录用户在本批需求下的未发布草稿，不影响其他用户草稿和已发布排班。
2. 生成的排班必须写入 `schedule`，且 `published=false`。
3. 生成前必须校验人员容量、保密权限、需求日期范围、排班百分比合法性。
4. 如果部分需求无法满足，允许返回未满足结果，但不能伪造满足状态。
5. 如果推荐计算超过 10 秒，后续版本可返回异步任务 ID。

##### 6.8.1.3 冲突检测接口

| 项 | 内容 |
| --- | --- |
| 方法 | `POST` |
| 路径 | `/schedules/conflicts/check` |
| 用途 | 对指定需求、排班或日期范围进行统一冲突检测 |
| 权限 | `resourceManager`、`projectManager`、`testLead` |

请求体：

```json
{
  "demandIds": [101, 102],
  "scheduleIds": [9001, 9002],
  "dateRange": {
    "startDate": "2026-07-08",
    "endDate": "2026-07-21"
  },
  "includeDrafts": true
}
```

返回：

```json
{
  "hasConflict": true,
  "conflicts": [
    {
      "type": "OVER_CAPACITY",
      "demandId": 101,
      "scheduleId": 9001,
      "staffId": 1,
      "staffName": "张三",
      "date": "2026-07-08",
      "currentLoadPercent": 150,
      "maxCapacityPercent": 100,
      "message": "张三在 2026-07-08 累计排班 150%，超过上限 100%"
    }
  ],
  "warnings": [
    {
      "type": "DEVICE_LIMIT",
      "demandId": 102,
      "message": "同一天并行人员数超过样机数量，首版作为警告处理"
    }
  ]
}
```

检测类型：

1. 单人单日累计超容量。
2. 非空闲状态容量不足。
3. 保密需求分配给无保密权限人员。
4. 测试类型明显不匹配。
5. 需求周期外排班。
6. 样机数量超限。

##### 6.8.1.4 批量发布接口

| 项 | 内容 |
| --- | --- |
| 方法 | `POST` |
| 路径 | `/schedules/publish-batch` |
| 用途 | 批量发布无冲突且人力满足的需求 |
| 权限 | `resourceManager`、`projectManager` |

请求体：

```json
{
  "demandIds": [101, 102, 103],
  "expectedRowVersions": [
    {
      "scheduleId": 9001,
      "rowVersion": 3
    }
  ]
}
```

返回：

```json
{
  "success": [
    {
      "demandId": 101,
      "publishedScheduleCount": 12
    }
  ],
  "failed": [
    {
      "demandId": 102,
      "reasonCode": "UNFULFILLED_MANPOWER",
      "message": "需求仍缺少 1.5 人天，已保留为草稿"
    },
    {
      "demandId": 103,
      "reasonCode": "CONFLICT",
      "message": "存在超容量冲突，无法发布"
    }
  ]
}
```

规则：

1. 批量发布采用逐条处理 + 收集成功/失败明细模式，不能因为单个需求失败导致全部回滚。
2. 有缺口需求不允许强制发布。
3. 有硬冲突需求不允许发布。
4. 发布成功后，将对应需求下当前用户草稿设置为 `published=true`。
5. 前端展示“成功 N 条、失败 M 条”，并列出失败原因。

##### 6.8.1.5 人员日期范围容量接口

| 项 | 内容 |
| --- | --- |
| 方法 | `GET` |
| 路径 | `/staff/availability?startDate=&endDate=&includeDrafts=` |
| 用途 | 聚合返回人员在日期范围内的容量、占用和冲突信息 |
| 权限 | `resourceManager`、`projectManager`、`testLead` |

返回：

```json
[
  {
    "staffId": 1,
    "staffName": "张三",
    "testType": "功能测试",
    "currentCoefficient": 1.0,
    "familiarModules": "登录,支付",
    "confidentialClearance": true,
    "dailyCapacity": {
      "2026-07-08": {
        "maxPercent": 100,
        "usedPercent": 50,
        "availablePercent": 50,
        "status": "AVAILABLE",
        "conflictDetails": []
      }
    }
  }
]
```

要求：

1. 用于减少前端对人员、排班、每日状态的多次请求和重复聚合。
2. `usedPercent` 至少包含已发布排班；是否包含草稿由 `includeDrafts` 控制。
3. 返回值必须包含匹配人员池所需的 `familiarModules` 和 `confidentialClearance`。

##### 6.8.1.6 安全清除需求草稿接口

现有 `DELETE /schedules/demand/{demandId}` 需要改造成默认保护已发布排班。

| 项 | 内容 |
| --- | --- |
| 方法 | `DELETE` |
| 路径 | `/schedules/demand/{demandId}?scope=draft_only` |
| 用途 | 清除指定需求下的排班，默认只清草稿 |
| 权限 | `resourceManager`、`projectManager` |

规则：

1. `scope=draft_only`：只删除当前登录用户创建的 `published=false` 草稿。
2. `scope=all`：必须二次确认，并且后端需要先取消发布或拒绝删除已发布排班。
3. 默认行为不得删除 `published=true` 的排班。

#### 6.8.2 补充测试申请新增接口

| 方法 | 路径 | 描述 |
| --- | --- | --- |
| POST | `/supplement-requests` | 提交补充测试申请 |
| GET | `/supplement-requests` | 查询补充测试申请 |
| PUT | `/supplement-requests/{id}/approve` | 批准补充测试申请 |
| PUT | `/supplement-requests/{id}/reject` | 驳回补充测试申请 |

#### 6.8.3 审计日志新增接口

| 方法 | 路径 | 描述 |
| --- | --- | --- |
| GET | `/audit-logs` | 查询操作日志 |

---

## 7. 数据库设计

### 7.1 数据库配置

当前配置：

| 配置项 | 值 |
| --- | --- |
| 数据库 | H2 文件数据库 |
| JDBC URL | `jdbc:h2:file:./data/testdb;MODE=MySQL;DB_CLOSE_DELAY=-1;AUTO_SERVER=TRUE` |
| 用户名 | `sa` |
| 密码 | 空 |
| H2 Console | `/h2-console` |
| JPA DDL | `update` |

### 7.2 表：users

用途：系统登录账号和用户基础信息。

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT | PK，自增 | 用户 ID |
| username | VARCHAR | 非空，唯一 | 登录名；人员账号使用工号 |
| password | VARCHAR | 非空 | BCrypt 密码 |
| display_name | VARCHAR | 可空 | 展示名 |
| test_type | VARCHAR | 可空 | 测试组长所属测试类型 |
| familiar_modules | VARCHAR(500) | 可空 | 熟悉模块 |
| confidential_clearance | BOOLEAN | 默认 false | 是否具备保密权限 |
| enabled | BOOLEAN | 默认 true | 是否启用；注册待审批为 false |
| created_at | DATETIME | 自动 | 创建时间 |

关系：

1. 与 `user_roles` 为一对多。
2. 与 `test_staff.emp_no` 通过用户名约定关联。

### 7.3 表：user_roles

用途：用户角色集合。

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| user_id | BIGINT | FK users.id | 用户 ID |
| role | VARCHAR | 非空 | 角色标识 |

角色值：

`testManager`、`resourceManager`、`projectManager`、`testExecutor`、`fieldAdmin`、`testLead`

### 7.4 表：test_staff

用途：测试人员资源池。

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT | PK，自增 | 人员 ID |
| name | VARCHAR | 非空 | 姓名 |
| emp_no | VARCHAR | 非空，唯一 | 工号 |
| join_date | DATE | 可空 | 入职日期 |
| group_name | VARCHAR | 可空 | 所属小组 |
| test_type | VARCHAR | 可空 | 测试类型 |
| familiar_modules | VARCHAR(500) | 默认空字符串 | 熟悉模块，多个值可用逗号分隔 |
| confidential_clearance | BOOLEAN | 默认 false | 是否具备保密需求测试权限 |
| initial_coefficient | DECIMAL(3,2) | 可空 | 初始系数 |
| current_coefficient | DECIMAL(3,2) | 可空 | 当前系数 |
| status | VARCHAR | 默认 active | active、leave、resigned |
| created_at | DATETIME | 自动 | 创建时间 |
| updated_at | DATETIME | 自动 | 更新时间 |

说明：

1. `familiar_modules` 和 `confidential_clearance` 必须持久化，匹配人员池、缺口分析、保密需求过滤依赖这两个字段。
2. `role`、`roles` 可继续通过关联 `users` 补充。
3. 当前系数由前端/管理人员维护，未实现自动递增任务。
4. 如果历史版本中 `familiarModules`、`confidentialClearance` 为实体瞬时字段，需要在后端实体、DTO、导入模板和人员编辑接口中同步改造。

### 7.5 表：test_demand

用途：测试需求主表。

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT | PK，自增 | 需求 ID |
| product | VARCHAR | 非空 | 产品名称 |
| version | VARCHAR | 可空 | 版本号 |
| start_date | DATETIME | 可空 | 测试开始时间 |
| end_date | DATETIME | 可空 | 测试结束时间 |
| manpower_demand | DECIMAL(10,2) | 可空 | 总人力需求，明细求和 |
| version_type | VARCHAR | 非空 | 版本类型 |
| version_phase | VARCHAR | 可空 | 版本阶段 |
| description | TEXT | 可空 | 备注说明 |
| status | VARCHAR | 默认 submitted | 需求状态 |
| submitted_by | VARCHAR | 可空 | 提交人 |
| confidential | BOOLEAN | 默认 false | 是否保密 |
| priority | VARCHAR | 可空 | 优先级 |
| test_device_count | INT | 可空 | 样机数量 |
| created_at | DATETIME | 自动 | 创建时间 |
| updated_at | DATETIME | 自动 | 更新时间 |

状态值：

`submitted`、`pending`、`scheduled`、`completed`、`rejected`

### 7.6 表：demand_manpower_detail

用途：需求按测试类型拆分的人力明细。

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT | PK，自增 | 明细 ID |
| demand_id | BIGINT | 非空 | 需求 ID |
| test_type | VARCHAR | 非空 | 测试类型 |
| manpower_demand | DECIMAL(10,2) | 可空 | 该测试类型所需人天 |
| remark | VARCHAR(200) | 可空 | 备注 |

关系：

1. `demand_id` 逻辑关联 `test_demand.id`。
2. 建议增加数据库外键或至少增加服务层级联维护，防止需求删除后留下孤立人力明细。
3. 人力排布工作台的缺口面板必须使用该表按测试类型计算缺口，不能只使用需求总人力。
4. 如果当前实体仍以 `@Transient manpowerDetails` 返回，需要在查询需求详情、待排需求列表时由后端主动装配明细。

### 7.7 表：schedule

用途：排班记录。

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT | PK，自增 | 排班 ID |
| demand_id | BIGINT | 可空 | 需求 ID |
| staff_id | BIGINT | 可空 | 人员 ID |
| date | DATE | 可空 | 排班日期 |
| percentage | INT | 可空 | 投入百分比 |
| product | VARCHAR | 可空 | 产品冗余字段 |
| test_manager | VARCHAR | 可空 | 测试经理 |
| version_type | VARCHAR | 可空 | 版本类型 |
| version | VARCHAR | 可空 | 版本号 |
| published | BOOLEAN | 默认 false | 是否发布 |
| created_by | VARCHAR | 可空 | 草稿创建人，用于多人排班时隔离草稿 |
| batch_id | VARCHAR | 可空 | 推荐草稿批次 ID，用于一次推荐结果追踪 |
| row_version | BIGINT | 默认 0 | 乐观锁版本号，避免与产品版本号 `version` 混淆 |
| created_at | DATETIME | 自动 | 创建时间 |
| updated_at | DATETIME | 自动 | 更新时间 |

说明：

1. 排班以“人员 + 日期 + 需求 + 百分比”为最小单位。
2. `published=false` 表示草稿。
3. `published=true` 表示任务看板可见。
4. `created_by` 用于保证用户 A 的草稿不会被用户 B 的清除草稿操作误删。
5. `row_version` 或 `updated_at` 用于发布、修改、删除时的并发校验。
6. 已发布排班不得通过按需求删除接口直接删除。

推荐索引：

1. `idx_schedule_demand_published`：`demand_id, published`。
2. `idx_schedule_staff_date`：`staff_id, date`。
3. `idx_schedule_created_by`：`created_by, demand_id, published`。
4. `idx_schedule_batch_id`：`batch_id`。

### 7.8 表：staff_daily_status

用途：维护人员某日非排班占用状态。

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT | PK，自增 | 状态 ID |
| staff_id | BIGINT | 非空 | 人员 ID |
| date | DATE | 非空 | 日期 |
| status | VARCHAR | 非空 | 每日状态 |
| percentage | DOUBLE | 默认 100 | 占用百分比 |

唯一约束：

`staff_id + date`

### 7.9 表：field_config

用途：基础字段配置。

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| id | BIGINT | PK，自增 | 配置 ID |
| field_name | VARCHAR | 非空 | 字段名 |
| field_type | VARCHAR | 非空 | 字段类型 |
| options | TEXT | 可空 | 选项，逗号分隔 |
| description | VARCHAR | 可空 | 描述 |
| required | BOOLEAN | 默认 false | 是否必填 |
| sort_order | INT | 可空 | 排序 |
| created_at | DATETIME | 自动 | 创建时间 |
| updated_at | DATETIME | 自动 | 更新时间 |

### 7.10 建议新增表：supplement_request

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT | 主键 |
| demand_id | BIGINT | 原需求 ID |
| reason | TEXT | 补充原因 |
| original_manpower | DECIMAL(10,2) | 原需求人力 |
| actual_manpower | DECIMAL(10,2) | 当前已投入人力 |
| extra_end_date | DATETIME | 补充结束日期 |
| extra_manpower | DECIMAL(10,2) | 补充人力需求 |
| status | VARCHAR | submitted、approved、rejected、changed_approved |
| submitted_by | VARCHAR | 提交人 |
| approved_by | VARCHAR | 审批人 |
| reject_reason | TEXT | 驳回原因 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### 7.11 建议新增表：audit_log

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | BIGINT | 主键 |
| operator | VARCHAR | 操作人 |
| action | VARCHAR | 操作类型 |
| target_type | VARCHAR | 对象类型 |
| target_id | BIGINT | 对象 ID |
| before_json | TEXT | 操作前数据 |
| after_json | TEXT | 操作后数据 |
| created_at | DATETIME | 操作时间 |

### 7.12 人力排布相关数据库改造清单

人力排布工作台重设计不是纯前端改造，后端数据结构至少需要补齐以下内容：

| 对象 | 改造项 | 必要性 |
| --- | --- | --- |
| `test_staff` | 增加 `familiar_modules` | 匹配人员池展示熟悉模块，并参与匹配分计算 |
| `test_staff` | 增加 `confidential_clearance` | 保密需求自动过滤和后端硬校验依赖 |
| `demand_manpower_detail` | 确认真实建表并可按需求查询 | 缺口必须按测试类型拆分 |
| `schedule` | 增加 `created_by` | 多人同时排班时隔离草稿 |
| `schedule` | 增加 `batch_id` | 推荐草稿批次追踪和异常回溯 |
| `schedule` | 增加 `row_version` 或使用 `updated_at` | 发布、修改、删除时做乐观锁 |
| `schedule` | 增加 `updated_at` | 前端检测数据是否过期 |

迁移要求：

1. 历史人员数据中缺少熟悉模块时，默认保存为空字符串。
2. 历史人员数据中缺少保密权限时，默认保存为 `false`。
3. 历史排班数据中缺少 `created_by` 时，可为空；新生成草稿必须写入当前登录用户。
4. 历史排班数据中缺少 `row_version` 时，默认从 0 开始。
5. 批量导入人员模板需要新增熟悉模块和保密权限列。
6. 人员编辑页面和人员接口需要同步读写新增字段。

---

## 8. 核心业务规则

### 8.1 人力需求求和规则

```
需求总人力 = sum(需求各测试类型人力)
```

如果明细为空，则总人力为 0。

### 8.2 排班人天计算

```
单条排班人天 = percentage / 100
需求已排人天 = sum(需求下排班 percentage / 100)
```

### 8.3 人员容量计算

```
人员单日原始容量 = currentCoefficient * 100
每日状态扣减后容量 = 原始容量 * (1 - 状态 percentage / 100)
```

状态为 `AVAILABLE` 时不扣减。

### 8.4 冲突判断

```
人员某日排班总百分比 > 人员当日可用容量
```

则判定冲突。

### 8.5 需求是否满足

```
需求已发布人天 >= 需求总人力
```

则需求发布排班满足。

在草稿场景中：

```
需求草稿+已发布人天 >= 需求总人力
```

则当前排班方案满足。

### 8.6 保密需求分配

1. 如果需求 `confidential=true`，只能推荐或分配给 `confidentialClearance=true` 的人员。
2. 前端在拖拽、分配、生成草稿前应阻止无权限人员进入推荐结果。
3. 后端在创建、批量创建、推荐草稿生成、发布前必须再次校验。
4. 不满足保密权限时返回业务错误，不能仅作为普通警告。

### 8.7 样机数量限制

1. 如果需求配置 `testDeviceCount`，同一需求同一天并行人员数不应超过样机数量。
2. 首版作为软约束：生成草稿和冲突检测时给出警告，但不阻止草稿生成。
3. 第二版升级为硬约束：推荐算法、拖拽分配、发布前校验均应阻止超限。

### 8.8 人力排布硬阻断规则

以下规则必须前后端双重校验。前端用于即时反馈，后端用于保证数据正确性。

| 规则 | 触发条件 | 前端行为 | 后端行为 |
| --- | --- | --- | --- |
| 单日累计超容 | 某人员某日排班总百分比 > `currentCoefficient * 100` | 分配弹窗、拖拽目标直接拒绝 | `create`、`createBatch`、`update`、`recommend/draft` 拒绝保存 |
| 保密权限不足 | `demand.confidential=true` 且人员无保密权限 | 人员不可选或拖拽失败 | 返回业务错误 |
| 已发布排班直接删除 | `published=true` 且未取消发布 | 隐藏或禁用删除入口 | 删除接口默认过滤已发布排班 |
| 需求周期外排班 | 排班日期早于需求开始或晚于需求结束 | 日期不可选 | 返回业务错误 |
| 缺口需求强制发布 | 需求草稿+已发布人天 < 需求总人力 | 发布按钮跳过该需求 | 批量发布返回失败原因 |

### 8.9 推荐草稿生成规则

1. 推荐草稿只处理用户本批选中的需求。
2. 已发布排班不被覆盖。
3. 非本批需求的草稿不被覆盖。
4. 当前用户在本批需求下的旧草稿可被清理后重建。
5. 固定人员优先参与推荐，但仍必须满足容量、权限和日期约束。
6. 排除人员不得进入推荐结果。
7. 推荐结果允许部分满足，但必须返回未满足需求及原因。
8. 生成草稿后，所有新增排班记录必须为 `published=false`。

### 8.10 批量发布规则

1. 批量发布只发布无冲突且人力满足的需求。
2. 有缺口需求保留为草稿，不允许强制发布。
3. 有硬冲突需求保留为草稿，不允许发布。
4. 批量发布采用部分成功/失败模式，不做全量事务回滚。
5. 发布成功的需求更新为已发布；发布失败的需求保持原草稿状态。
6. 前端必须展示成功数量、失败数量和逐项失败原因。

### 8.11 人员容量聚合规则

```
人员单日最大容量百分比 = currentCoefficient * 100
人员单日已占用百分比 = 已发布排班百分比 + 可选的当前用户草稿百分比 + 每日状态占用百分比
人员单日可用百分比 = 最大容量百分比 - 已占用百分比
```

说明：

1. 每日状态为 `AVAILABLE` 时不扣减容量。
2. 每日状态为请假、借调、其他任务时，按 `percentage` 扣减容量。
3. 匹配人员池的可用率应以目标日期范围内的汇总容量计算。
4. 后端容量接口应能返回日期粒度的 `maxPercent`、`usedPercent`、`availablePercent` 和状态。

### 8.12 注册审批规则

1. 新注册用户默认禁用。
2. 审批通过后启用。
3. 拒绝则删除用户。

---

## 9. 非功能需求

### 9.1 性能

| 指标 | 要求 |
| --- | --- |
| 登录响应 | 1 秒内 |
| 普通列表加载 | 2 秒内 |
| 排班工作台初次加载 | 3 秒内 |
| 拖拽交互反馈 | 100ms 内 |
| 推荐排班生成 | 5 秒内 |
| 任务看板周视图渲染 | 2 秒内 |

### 9.2 安全

1. 密码必须使用 BCrypt 存储。
2. 接口需校验 JWT。
3. 前端按钮级权限不能替代后端接口级权限。
4. 后续应补充后端角色注解或拦截器权限校验。
5. 禁止在 Git 远程地址中长期保存明文 token。

### 9.3 可用性

1. 所有保存、删除、审批、发布操作都要有成功或失败提示。
2. 高风险操作必须二次确认。
3. 空数据要有明确说明。
4. 表格需要分页或滚动固定表头。
5. 工作台需要固定顶部操作区，避免滚动后丢失上下文。

### 9.4 兼容性

1. 支持 Chrome 最新版。
2. 支持 Safari 最新版。
3. 支持 1280px 及以上桌面宽度。
4. 排班工作台优先保证桌面端体验。

### 9.5 可维护性

1. 建议将当前大型页面拆分组件。
2. 建议将排班计算逻辑从 UI 组件中抽离。
3. 建议将接口类型显式声明，减少 `any`。
4. 建议为核心业务规则补单元测试。

---

## 10. 验收标准

### 10.1 登录与权限

- [ ] 用户可使用有效账号登录。
- [ ] 禁用账号不能登录。
- [ ] 登录后默认进入首页。
- [ ] 侧边栏只展示当前用户有权限的菜单。
- [ ] 审批中心徽标能展示待审批数量。

### 10.2 测试需求

- [ ] 用户可提交带测试类型明细的需求。
- [ ] 总人力自动等于明细求和。
- [ ] 项目经理可批准、退回、修改后批准。
- [ ] 已批准需求进入待排期列表。
- [ ] 用户可关闭需求。
- [ ] 用户可导出需求。

### 10.3 人力排布

- [ ] 用户可按需求创建排班草稿。
- [ ] 用户可批量生成推荐草稿。
- [ ] 用户进入工作台后能看到待排需求数量、高风险需求数量、草稿数量和可发布需求数量。
- [ ] 待排需求默认按风险排序，支持按测试类型、优先级、产品、版本类型、截止时间、保密状态筛选。
- [ ] 用户选择一批需求后，系统能展示匹配人员池、人员匹配原因、可用率和预计缺口。
- [ ] 生成推荐草稿前，用户能看到可行性预估，且该步骤不写入数据库。
- [ ] 点击生成推荐草稿后，后端创建 `published=false` 的排班记录。
- [ ] 用户可拖拽移动排班。
- [ ] 用户可编辑排班百分比。
- [ ] 用户可删除排班。
- [ ] 用户可检测冲突。
- [ ] 超容量、保密权限不足、需求周期外排班在前端和后端均被阻断。
- [ ] 缺口和冲突集中显示在缺口与发布面板。
- [ ] 用户可发布指定需求排班。
- [ ] 用户可批量发布无冲突且已满足的需求。
- [ ] 批量发布部分失败时，成功需求正常发布，失败需求保留草稿并显示原因。
- [ ] 有缺口需求不能强制发布。
- [ ] 已发布排班出现在任务看板。
- [ ] 已发布排班不能被按需求删除接口直接删除。
- [ ] 用户刷新或离开页面前，系统能提示未完成排布操作，并支持恢复本地操作上下文。
- [ ] 多人同时排布时，用户只能清理自己的草稿，不会误删其他用户草稿。

### 10.4 人员管理

- [ ] 用户可新增人员并生成账号。
- [ ] 用户可编辑人员信息、角色、熟悉模块、保密权限。
- [ ] 用户可批量导入人员。
- [ ] 删除人员时同步删除账号。
- [ ] 测试组长不能删除人员。

### 10.5 每日状态

- [ ] 测试组长或字段管理员可设置人员每日状态。
- [ ] 非空闲状态会影响工作台可用容量。
- [ ] 任务看板展示每日状态。

### 10.6 报表和仪表盘

- [ ] 仪表盘展示核心指标。
- [ ] 仪表盘图表基于真实需求、排班、人员数据计算。
- [ ] 报表可按产品统计计划人力和实际投入。

---

## 11. 当前问题与重构建议

### 11.1 当前问题

1. 部分页面职责过重，尤其是人力排布工作台。
2. 前端存在大量 `any`，接口类型不稳定。
3. 推荐排班、冲突检测等核心业务逻辑主要在前端，后端缺少统一规则入口。
4. 补充测试申请未落后端。
5. 删除需求未显式级联删除人力明细和排班。
6. 后端接口缺少细粒度权限控制。
7. 当前系数需要手动维护，未实现按入职日期自动递增。
8. 操作日志缺失。

### 11.2 重构优先级

P0：

1. 拆分人力排布工作台。
2. 抽离排班计算逻辑。
3. 补充后端冲突检测和推荐预览接口。
4. 明确排班发布规则和状态机。

P1：

1. 完成补充测试申请后端化。
2. 增加接口级权限控制。
3. 增加操作日志。
4. 需求删除级联处理。

P2：

1. 自动计算新员工系数。
2. 报表增加更多维度。
3. 接入企业 SSO。
4. 外部通知能力。

---

## 12. 附录

### 12.1 关键文件映射

| 功能 | 前端文件 | 后端文件 |
| --- | --- | --- |
| 登录注册 | `src/pages/LoginPage.tsx`, `src/pages/RegisterPage.tsx` | `AuthController`, `AuthService`, `User` |
| 首页 | `src/pages/Home.tsx` | 无直接后端 |
| 需求管理 | `src/pages/TestDemandList.tsx`, `src/pages/TestDemandSubmit.tsx` | `TestDemandController`, `TestDemandService`, `TestDemand`, `DemandManpowerDetail` |
| 需求审批 | `src/pages/DemandApproval.tsx` | `TestDemandController`, `TestDemandService` |
| 排班工作台 | `src/pages/ScheduleWorkbench.tsx` | `ScheduleController`, `ScheduleService`, `Schedule` |
| 任务看板 | `src/pages/TaskKanban.tsx` | `ScheduleController`, `TestStaffController`, `StaffDailyStatusController` |
| 人员管理 | `src/pages/StaffManagement.tsx` | `TestStaffController`, `TestStaffService`, `TestStaff`, `User` |
| 每日状态 | `ScheduleWorkbench.tsx`, `TaskKanban.tsx` | `StaffDailyStatusController`, `StaffDailyStatusService`, `StaffDailyStatus` |
| 字段配置 | `src/pages/BaseConfig.tsx` | `FieldConfigController`, `FieldConfigService`, `FieldConfig` |
| 仪表盘 | `src/pages/Dashboard.tsx` | 复用需求、排班、人员接口 |
| 报表 | `src/pages/Reports.tsx` | 复用需求、排班接口 |
| 审批中心 | `src/pages/ApprovalCenter.tsx`, `RegistrationApproval.tsx`, `DemandApproval.tsx` | `AuthController`, `TestDemandController` |

### 12.2 术语表

| 术语 | 含义 |
| --- | --- |
| 人天 | 一个完整人力工作日。50% 一天等于 0.5 人天 |
| 草稿排班 | 未发布排班，`published=false` |
| 已发布排班 | 可被任务看板看到的排班，`published=true` |
| 当前系数 | 人员当前可投入能力，如 0.7 表示 70% |
| 每日状态 | 请假、借调、其他任务等非排班占用 |
| 高风险需求 | 临近截止、剩余人天大、资源缺口明显的需求 |
| 保密需求 | 只能分配给具备保密权限人员的需求 |

---

## 13. 文档结论

该系统已经具备完整业务雏形，但随着功能叠加，页面和业务规则开始集中在少数大文件中。后续应以本 PRD 为业务边界，优先重构人力排布工作台和排班规则层，将“需求、人员、排班、状态、发布、统计”的职责拆清楚，再逐步补齐后端规则接口和数据治理能力。
